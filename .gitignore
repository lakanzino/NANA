# ── فایل‌های موقت ──
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# ── خروجی‌های بازتولیدشدنی ──
# PNG اصلی تصاویر شاخص — نسخهٔ WebP در thumbs-webp/ کافی است
thumbs/

# گزارش HTML که با اسکریپت ساخته می‌شود
audit/index.html

# ── نکته ──
# زیپ افزونه‌ها با فرمان zip از پوشهٔ هم‌نامشان بازساخته می‌شود.
# فقط بسته‌های اجرانشده در مخزن نگه داشته می‌شوند.
