# دستورالعمل مقاله‌نویسی QPedia — نسخهٔ نهایی و الزام‌آور

**وب‌سایت:** `https://qpedia.ir`
**نسخه:** ۱.۵ — ۱۵ شهریور ۱۴۰۵ (۲۰۲۶-۰۹-۰۶)
**تغییرات نسخهٔ ۱.۵:** بازنویسی کامل بخش ۱۲ (تصویر شاخص و متن جایگزین)، افزودن بخش ۱۸ (کف واژگان)، بخش ۱۹ (**صدای نویسنده** — لغو حذف لحن محاوره‌ای)، بخش ۲۰ (متادسکریپشن)، بخش ۲۱ (قاعدهٔ بازبینی مقالات موجود).
**تغییرات نسخهٔ ۱.۴:** افزودن بخش ۱۷ — **برداشتن سقف ۷ لینک داخلی** و **الزامی شدن لینک دانشمندان** (یک بار به ازای هر دانشمند، مستثنا از سقف).
**تغییرات نسخهٔ ۱.۳:** افزودن بخش ۱۴ (منابع لینک‌دار و تأییدشده)، بخش ۱۵ (قاعدهٔ مضمون به‌جای نقل‌قول)، بخش ۱۶ (اصلاح ورودی خام) + به‌روزرسانی خط قرمز ۲ و چک‌لیست بخش ۷.
**وضعیت:** ✅ تأییدشده — تنها مرجع نوشتن تمام مقالات (جایگزین `قالب-مقاله-نویسی-QPedia.md`)
**مبنای تدوین:** ۱) قالب واقعی مقالات منتشرشدهٔ سایت زنده ۲) قواعد اعلامی مدیر پروژه
**مخاطب:** هر کس (انسان یا ابزار) که متن مقاله‌ای برای QPedia می‌نویسد.

---

## قانون صفر — این سند «زنده» است و همیشه مقدم است

> هر بار قاعدهٔ جدیدی در پروژه اضافه می‌شود، **اول در همین سند ثبت می‌شود، بعد اجرا می‌شود.**
> قبل از نوشتن هر مقاله یا شروع هر کار جدید، همین سند دوباره خوانده و چک‌لیست بخش ۷ تیک زده می‌شود.

این سند یک‌بار مصرف نیست؛ با رشد پروژه نسخه‌دار می‌شود و هیچ قاعده‌ای خارج از آن اعمال نمی‌گردد.

---

## بخش ۰ — هدف پروژه (چرا این قواعد وجود دارند)

> **هدف انتقال «حسِ» عجیب بودن است، نه انتقال اطلاعات.**
> اطلاعات در ویکی‌پدیا هست؛ چیزی که نیست، لحظهٔ «صبر کن، چی؟!» است.

هر مقاله یک **تجربهٔ خوانشی** است، نه گزارش دانشنامه‌ای خشک.

---

## بخش ۱ — قاعدهٔ طلایی (اولین بررسی قبل از هر چیز)

> هر مقاله باید یک لحظهٔ **«صبر کن، چی؟!»** داشته باشد.
> اگر خواننده تا پایان یک‌بار هم جا نخورد، مقاله شکست خورده است.

**اجرای عملی:** این لحظه معمولاً در یکی از سه نقطه ساخته می‌شود:
1. **قلابِ لید** (جملهٔ اول) — یک متناقض‌نما یا ادعای باورنکردنیِ درست.
2. **قیاس وسط مقاله** — قیاسی که شهود خواننده را واژگون می‌کند.
3. **جمع‌بندی** — یک جملهٔ پایانی که کل تصویر قبلی را جابه‌جا می‌کند.

**تست پذیرش:** اگر هیچ‌جای مقاله پیدا نمی‌کنی که خواننده مکث کند → مقاله بازنویسی می‌شود، منتشر نمی‌شود.

---

## بخش ۲ — اصول قبلی به ترتیب (قالب مقالات منتشرشدهٔ سایت زنده)

### ۱) تیتر سئو
- فارسی، حاوی کلمهٔ کلیدی اصلی؛ ترجیحاً **پرسشی** یا **ادعای شگفت‌آور**.
- **طول: زیر ۶۰ کاراکتر** (اصلاح‌شده نسبت به نسخهٔ قبلی؛ سقف ۶۰ الزامی است).
- مثال زنده: «فوتون دقیقاً چیست؟» / «اصل طرد پاولی دقیقاً چه می‌گوید؟ به زبان ساده»

### ۲) اسلاگ
- **لاتین، کوتاه، بدون stop word** (بدون a/the/of/to/with…).
- ۲ تا ۳ کلمه با خط تیره.
- مثال: `photon`، `qubit`، `double-slit-experiment`

### ۳) اگزسرپت / توضیح متا
- دقیقاً ۲ جمله: جملهٔ اول **قلاب**، جملهٔ دوم **پوشش مقاله** («این مقاله … را بررسی می‌کند»).
- **طول: ۱۲۰ تا ۱۵۵ کاراکتر.**
- این متن در وردپرس همان توضیح متای صفحه می‌شود.

### ۴) لید (پاراگراف اول)
- شروع با **صحنهٔ روزمره و ملموس**، نه تعریف خشک.
- رسیدن به مفهوم اصلی در همان پاراگراف اول.
- مثال زنده: «چراغی را روشن می‌کنیم، اتاق یک‌باره روشن می‌شود… اما همین نور وقتی پای آزمایش دقیق وسط می‌آید…»

### ۵) بدنه: تیترهای H2 پرسشی
- ۳ تا ۶ بخش؛ هر بخش پاسخ یک سؤال واقعی خواننده.
- **در بدنه `H1` نوشته نمی‌شود** — وردپرس خودش H1 را از عنوان می‌سازد؛ تیترگذاری از H2 شروع می‌شود.

### ۶) قیاس ملموس (الزامی، پیش از هر مفهوم دشوار)
- هر مفهوم انتزاعی ابتدا با یک قیاس از زندگی روزمره توضیح داده می‌شود؛ **قیاس قبل از فرمول**.
- مثال زنده: «فوتون برای نور همان نقش را دارد که "دانه" برای بارش تگرگ…»

### ۷) نقل‌قول کلیدی (Blockquote)
- یک باور غلط رایج + تصحیحش در قالب نقل‌قول.

### ۸) بولدکردن اصطلاحات کلیدی
- اولین ظهور هر اصطلاح علمی در متن **بولد** می‌شود.

### ۹) طول مقاله
- **راهنمای هدف: ۱۲۰۰ تا ۱۸۰۰ کلمه** (اصلاح‌شده ۱۱ شهریور: این بازه «راهنما» است نه شرط سخت — **کیفیت، مفهوم، ساده‌سازی با مثال و تشبیه و پرسش مهم‌تر از شمارش دقیق کلمات است**؛ متن هرگز با پرکننده آب‌بندی نمی‌شود).

### ۱۰) لحن
- طبق جدول بخش ۳. فارسی روان و معیار؛ راوی دوم‌شخص؛ صادق دربارهٔ مرزهای دانش.

### ۱۱) ساختار متن تحویلی (HTML سادهٔ آماده — اصلاح ۱۲ شهریور)
> **چرا HTML؟** تست انتشار واقعی نشان داد وردپرس Markdown را خودکار تبدیل نمی‌کند و مقاله به‌صورت متن سادهٔ پشت‌سرهم منتشر شد. بدنه از این به بعد **HTML سادهٔ آمادهٔ چسباندن** است.
```
(تیتر سئو در فیلد عنوان وردپرس — داخل بدنه نمی‌آید)
(اگزسرپت در فیلد اختصاصی وردپرس — نه داخل بدنه)

<p>پاراگراف لید…</p>

<h2>سؤال بخش ۱</h2>
<p>…</p>

<blockquote><p>نقل‌قول کلیدی</p></blockquote>

<h2>سؤال بخش ۲</h2>
<p>…</p>

<h2>جمع‌بندی</h2>
<p>…</p>

<h2>سوالات متداول</h2>
<details><summary><strong>۱. سؤال؟</strong></summary><p>پاسخ کوتاه.</p></details>
<details><summary><strong>۲. سؤال؟</strong></summary><p>پاسخ کوتاه.</p></details>

<h2>منابع</h2>
<ol><li>…</li></ol>
```
- **ترتیب ثابت بخش‌ها (قانون ۱۲ شهریور):** لید ← بخش‌های H2 ← جمع‌بندی ← **سوالات متداول (آکاردئونی)** ← **منابع همیشه آخرین بخش.**
- تگ‌های مجاز: `h2` `h3` `p` `strong` `em` `blockquote` `ul` `ol` `li` `a` `details` `summary` — آکاردئون سوالات متداول فقط با `details/summary` ساخته می‌شود (بدون هیچ اسکریپت؛ با لمس باز و بسته می‌شود).
- ممنوع: `div` `span` `style` `class` و هر CSS درون‌متنی.
- روش چسباندن: ویرایشگر در حالت «متن/HTML» (کلاسیک) یا بلوک «HTML سفارشی» (گوتنبرگ).
- **فاصله‌گذاری (قانون ۱۲ شهریور):** بین اجزای کلمات همیشه **فاصلهٔ کامل و واقعی** — «تله پورت»، «شبیه سازی»، «انگلیسی زبان»، «اندازه گیری»، «بی ذهن»، «می شود». نیم‌فاصله (ZWNJ) در مسیر کپی از چت به وردپرس حذف می‌شود و کلمات چسبیده منتشر می‌شوند؛ استفاده از نیم‌فاصله در متن تحویلی ممنوع است.
## منابع
```

### ۱۲) متادیتای انتشار
| فیلد | مقدار |
|---|---|
| post_type | `quantum_article` |
| دسته | فقط از دسته‌های درختچهٔ رسمی سایت |
| اسلاگ | لاتین کوتاه بدون stop word |
| تصویر شاخص | کاور ۱۶:۹ سری QPedia |
| جایگاه انتشار | طبق ستون درختچه (بخش ۶) |

---

## بخش ۳ — جدول لحن

| بکن | نکن |
|---|---|
| دوم‌شخص: «تصور کنید»، «فرض کنید» | «همان‌طور که می‌دانیم» |
| جملهٔ کوتاه؛ یک ایده در هر پاراگراف | پاراگراف ده‌خطی |
| قیاس ملموس **پیش از** فرمول | فرمول بدون زمینه |
| «هنوز نمی‌دانیم» وقتی واقعاً نمی‌دانیم | قطعیت جعلی |
| نام فیزیک‌دان + سال دقیق | «دانشمندان می‌گویند» |
| رد صریح ادعاهای شبه‌علمی | سکوت مؤدبانه در برابرشان |

---

## بخش ۴ — سه خط قرمز (تخلف = بازنویسی اجباری)

**۱. متن خام هوش مصنوعی منتشر نشود.**
گوگل از مارس ۲۰۲۴ روی محتوای انبوه ماشینی سخت‌گیر است. حداقل کار:
- قلاب ابتدایی (پاراگراف اول) باید بازنویسی انسانی/دستی باشد؛
- یک **مثال بومی ایرانی** داخل متن گذاشته شود (چای، بازار، قنات، مدرسه، مسیر تهران-کرج…).

**۲. E-E-A-T — مرجع واقعی و لینک‌دار.**
پایین هر مقالهٔ علمی **حداقل سه مرجع واقعی**: مقالهٔ اصلی، کتاب درسی، یا صفحهٔ نوبل.
ویکی‌پدیا به‌تنهایی کافی نیست.
**هر مرجع باید لینک واقعی و باز‌شونده داشته باشد** — جزئیات الزامی در بخش ۱۴.

**۳. ترتیب انتشار ستون به ستون.**
اگر پراکنده منتشر شود، لینک‌های داخلی به مقالات منتشرنشده می‌خورند و گراف دانش شکل نمی‌گیرد.
هر مقاله فقط بعد از مقاله‌هایی منتشر می‌شود که به آن‌ها لینک داخلی دارد.

**۴. متن تحویلی فقط HTML سادهٔ آماده است — بدون هیچ CSS.**
قالب QPedia خودش استایل می‌گذارد؛ هر استایل دستی داخل مقاله چیدمان را می‌ریزد به هم.
- مجاز: `h2` `h3` `p` `strong` `em` `blockquote` `ul` `ol` `li` `a` (لینک با href).
- ممنوع: `div` و `span`، ویژگی `style=`، کلاس‌ها، `<style>`، `<font>`، رنگ/اندازهٔ دستی، و هر CSS درون‌متنی.
- اگر مفهومی به استایل خاص نیاز دارد، با «تیتر H2 + نقل‌قول + لیست» همان حس ساخته می‌شود — نه با CSS.

---

## بخش ۵ — سئو (مشخصات الزامی هر مقاله)

| مورد | مقدار |
|---|---|
| طول متن | ۱۲۰۰ تا ۱۸۰۰ کلمه |
| اسلاگ | لاتین، کوتاه، بدون stop word |
| عنوان | زیر ۶۰ کاراکتر |
| توضیح متا | ۱۲۰ تا ۱۵۵ کاراکتر |
| H1 در بدنه | **ممنوع** — وردپرس خودش می‌سازد (از H2 شروع کنید) |
| لینک داخلی | **۳ تا ۶ لینک** در هر مقاله، فقط به مقالات منتشرشدهٔ (✅) درختچه |
| فرمت تحویل | **HTML سادهٔ آماده** — فقط تگ‌های مجاز، بدون div/span/style/class |

---

## بخش ۶ — ترتیب انتشار (ستون به ستون)

- ستون انتشار در درختچهٔ رسمی (`درختچه-کامل-مقالات-QPedia.md`) تعیین می‌شود.
- قانون: **مقالهٔ جدید بعد از همهٔ مقاله‌هایی که به آن‌ها لینک می‌دهد منتشر می‌شود.**
- لینک داخلی فقط به مقالات با علامت ✅ (منتشرشده) مجاز است.
- پیش از انتشار هر مقاله: چک شود همهٔ مقاصد لینک داخلی‌اش زنده‌اند.

---

## بخش ۷ — چک‌لیست اجرایی نهایی (قبل از تحویل هر مقاله)

- [ ] لحظهٔ «صبر کن، چی؟!» وجود دارد و جای آن مشخص است.
- [ ] قلاب ابتدایی دست‌نویس/انسانی است، نه متن خام ماشینی.
- [ ] حداقل یک مثال بومی ایرانی در متن هست.
- [ ] قیاس ملموس پیش از هر فرمول/مفهوم دشوار.
- [ ] **حداقل ۳ مرجع واقعی و لینک‌دار** در بخش منابع؛ هر لینک تست‌شده و باز‌شونده (بخش ۱۴).
- [ ] هیچ منبعی «نام دامنهٔ خالی» یا لینک بی‌ربط نیست؛ مقالات علمی با `https://doi.org/<DOI>` آمده‌اند.
- [ ] DOIها در Crossref راستی‌آزمایی شده‌اند (عنوان + مجله + سال منطبق).
- [ ] هیچ نقل‌قول مستقیمِ بدون منبع مکتوب در متن نیست (بخش ۱۵).
- [ ] عنوان زیر ۶۰ کاراکتر، فارسی، حاوی کلیدواژه.
- [ ] اسلاگ لاتین کوتاه بدون stop word.
- [ ] اگزسرپت ۱۲۰ تا ۱۵۵ کاراکتر (قلاب + پوشش).
- [ ] هیچ H1 در بدنه نیست؛ بخش‌ها H2 پرسشی‌اند (۳ تا ۶ بخش).
- [ ] ۳ تا ۶ لینک داخلی، فقط به مقالات منتشرشدهٔ ✅ درختچه.
- [ ] طول بین ۱۲۰۰ تا ۱۸۰۰ کلمه.
- [ ] لحن: دوم‌شخص، جملهٔ کوتاه، «هنوز نمی‌دانیم» صادقانه.
- [ ] نام فیزیک‌دان + سال دقیق (نه «دانشمندان می‌گویند»).
- [ ] رد صریح شبه‌علم در صورت برخورد با آن.
- [ ] دسته و جایگاه ستون از درختچهٔ رسمی تعیین شده است.
- [ ] فایل تحویلی **HTML سادهٔ آماده** است: فقط تگ‌های مجاز (h2/h3/p/strong/em/blockquote/ul/ol/li/a/details/summary)، بدون div/span/style/class.
- [ ] ترتیب بخش‌ها درست است: جمع‌بندی ← سوالات متداول آکاردئونی (details/summary) ← منابع در آخر.
- [ ] **هیچ نیم‌فاصله (ZWNJ) در متن نیست** — همهٔ اجزای کلمات با فاصلهٔ کامل جدا شده‌اند («تله پورت» نه «تلهپورت»).
- [ ] قواعد جدید (اگر این مقاله قاعده‌ای ایجاب کرد) **اول در همین سند ثبت شده‌اند**.
- [ ] لحن معلمانه: حداقل یک اصطلاح فارسی بخش ۹ در متن هست (هیچ‌کدام بیش از یک بار).
- [ ] مثال‌ها ایرانی‌اند (شهر/استان/مکان واقعی ایران یا نام اصیل ایرانی).
- [ ] نقل‌قول‌ها فقط معتبر و تأییدشده‌اند؛ هیچ نقل‌قول مشکوک/جعلی نیست.
- [ ] هر فرمول با جملهٔ «فقط برای اینکه بدونید چه شکلیه…» همراه است.
- [ ] موضع علمی: درک درست + ضدخرافه + «هنوز خیلی مانده تا حقیقت را بدانیم» صادقانه.
- [ ] بخش پرسش و پاسخ پایانی (۳–۴ سؤال) در انتهای مقاله هست.
- [ ] چکیده/اگزسرپت (۱۲۰–۱۵۵ کاراکتر) و اسلاگ جدا در انتهای بستهٔ تحویل آمده‌اند.
- [ ] تصویر شاخص: WebP بهینه‌شده با نام مقاله، همراه بستهٔ تحویل است.

---

## بخش ۸ — چرا مثال ملموس این‌قدر تأکید شده؟

> چون هدف پروژه انتقال **حسِ** عجیب بودن است، نه انتقال اطلاعات.
> اطلاعات در ویکی‌پدیا هست؛ چیزی که نیست، لحظهٔ «صبر کن، چی؟!» است.

مثال ملموس تنها ابزاری است که فیزیک کوانتوم را از «فرمول» به «تجربهٔ ذهنی» تبدیل می‌کند — پس قبل از هر اصطلاح علمی، یک تصویر روزمره.

---

## بخش ۹ — لحن معلمانهٔ فارسی (پرسونال‌سازی متن) — مصوب ۱۰ شهریور ۱۴۰۵

هدف: متن مثل یک معلم خوب فارسی حرف بزند، نه مثل گزارش ماشینی. از این اصطلاحات **با خلاقیت و در جای درست** استفاده می‌شود — **هر عبارت حداکثر یک بار در هر مقاله**:

- «خوب یه جمع‌بندی کنیم…»
- «بچه‌ها توجه کنید اینجا…»
- «اگه یادتون باشه گفتیم که…»
- «ببینید…»
- «اینجا رو حواستون رو جمع کنید، که متوجه قضیه بشید…»
- «بریم سر اصل داستان…»
- «خوب تا اینجا که فکر کنم مشکلی نباشه، بریم ادامهٔ مبحث…»
- «ببینید بچه‌ها، می‌دونم کمی پیچیده به نظر میاد، علم کوانتوم همینه… اما جذابیتش هم همینه…»

**مثال‌ها باید ایرانی باشند:** نام شهرها، استان‌ها و مکان‌های واقعی و فعلی ایران + نام‌های اصیل ایرانی در مثال‌ها (مثلاً بازار تبریز، متروی تهران، کویر لوت، پل خواجو اصفهان، چایخانهٔ سنتی…).

---

## بخش ۱۰ — نقل‌قول، فرمول، و موضع علمی

**نقل‌قول:** فقط نقل‌قول‌هایی که به صحتشان مطمئن هستیم و به جذابیت آموزشی کمک می‌کنند، هایلایت می‌شوند و در Blockquote می‌آیند. نقل‌قول مشکوک/جعلی ممنوع است (مخصوصاً نقل‌قول‌های جعلی منسوب به اینشتین).
> ⚠️ **از نسخهٔ ۱.۳ این بند تشدید شده است** — معیار «مطمئن بودن» دیگر کافی نیست؛ **بخش ۱۵** حاکم است: هر نقل‌قول باید منبع مکتوبِ لینک‌دار داشته باشد، وگرنه به «مضمون مستند» تبدیل یا حذف می‌شود.

**فرمول:** هر جا فرمولی لازم شد، همیشه این جملهٔ صادقانه می‌آید: «این فرمول فقط برای اینکه بدونید چه شکلیه، و وارد شکل تخصصی‌اش نمی‌شیم.» — سپس قیاس جای محاسبه را می‌گیرد.

**موضع علمی ثابت:**
- هدف، رسیدن به **درک درست از کوانتوم** است؛ طوری که خواننده **وارد خرافات نشود**.
- کوانتوم علم اثبات‌شده است و فناوری‌های واقعی دارد؛ **اما هنوز خیلی مانده تا حقیقت کاملش را بدانیم** — این جمله را می‌شود و باید صادقانه گفت.

---

## بخش ۱۱ — بستهٔ تحویل هر مقاله (فرمت ارسال به مالک — اصلاح ۱۱ شهریور)

**روال تحویل:** پردازش **مقاله‌به‌مقاله** است؛ مقالهٔ بعدی فقط بعد از تأیید مالک پردازش می‌شود. هر مقاله در سه بخش جدا تحویل می‌شود:

1. **متن کامل مقاله داخل «باکس کپی»** (کدبلاک) — و فقط همان متنی که در بدنهٔ وردپرس جایگذاری می‌شود؛ بدون تیتر H1 (تیتر در فیلد عنوان می‌رود) و بدون بلوک «بستهٔ انتشار». داخل باکس: لید + بخش‌های H2 + جمع‌بندی + منابع + پرسش‌وپاسخ پایانی. **فرمت باکس: HTML سادهٔ آماده برای کپی مستقیم** (جایگذاری در حالت متن/HTML).
2. **بلوک مشخصات انتشار بیرون از باکس در چت** — تایتل، دسته، زیردسته (اگر دارد)، اسلاگ انگلیسی، چکیده/متا، نام تصویر.
3. **تصویر شاخص** — از نمایشگر فایل دانلود می‌شود (اگر در دسترس نبود، نبودنش مانع کار نیست).

**قاعدهٔ متای گوگل (تصویب ۱۱ شهریور):**
- چکیده در فیلد **«چکیده/Excerpt» وردپرس** وارد می‌شود — **نه داخل متن بدنه**.
- دلیل: خزندهٔ گوگل متای توضیحات را از تگ `<meta name="description">` در `head` صفحه می‌خواند، نه از متن بدنه. سایت QPedia **از قبل** این کار را خودکار انجام می‌دهد (تابع `qpedia_auto_seo_meta_tags` در `functions.php` سرور: متا + OpenGraph + Twitter از فیلد چکیده می‌سازد؛ اگر چکیده خالی باشد ۳۰ کلمهٔ اول بدنه را برمی‌دارد). پس با پر کردن فیلد چکیده، خواستهٔ «متا را خزنده بداند» دقیقاً برآورده می‌شود — بدون اینکه متن اضافه‌ای در مقاله دیده شود.
- هرگز تگ HTML یا متن متا به‌صورت مرئی داخل بدنه چسبانده نمی‌شود.

**تکراری‌ها (تصویب ۱۱ شهریور):** حذف قطعی. سه نوع:
- زوج‌های داخلی `(1)` — مقایسه و نگه‌داشتن یک نسخه (حذف شدند).
- **تکراری کامل مقالهٔ منتشرشدهٔ سایت** — حذف از کورپوس (مثال: `فتوسنتز؛ کارآمدترین ماشین جهان (1)` = عین مقالهٔ زندهٔ `quantum-photosynthesis` → حذف شد).
- هم‌موضوع ولی با زاویهٔ متفاوت — پردازش می‌شود با زاویهٔ متفاوت و لینک به مقالهٔ قبلی.

---

## بخش ۱۲ — تصویر شاخص و متن جایگزین (بازنویسی‌شده — نسخه ۱.۵)

### ۱۲.۱ سبک بصری (مصوب ۱۵ شهریور ۱۴۰۵)

- پس‌زمینهٔ کاغذی روشن `#f4f3ef`
- دیاگرام‌های علمی خطی **فقط در حاشیه‌ها و گوشه‌ها** — مرکز کاملاً خالی
- کلیدواژهٔ **انگلیسی** در مرکز، فونت **Orbitron Black**، سرمه‌ای `#14315c`
- **یک واژه بهتر است؛ حداکثر سه واژه.** واژه باید موضوع را نشان دهد،
  نه ترجمهٔ تحت‌اللفظی عنوان فارسی

### ۱۲.۲ خط اسکن

یک **خط نازک درخشان** از روی نوشته رد می‌شود:

- **فقط به عرض نوشته** — نه سراسر بنر
- ارتفاع **متنوع**: وسط، یک‌سوم بالا، یا یک‌سوم پایین (همه یکسان نباشد)
- رنگ از هشت گزینه، **متناسب با موضوع**: قرمز برای هشدار و نقد،
  سرخابی برای پزشکی، طلایی برای نوبل، سبز برای مرز و رشد، و…

### ۱۲.۳ فرمت خروجی

| نسخه | فرمت | ابعاد | حجم هدف |
|---|---|---|---|
| بایگانی | PNG | ۱۴۵۶×۷۲۸ | — |
| **آپلود** | **WebP** | **۹۰۰×۴۵۰** | **زیر ۵۰ KB** |

نام فایل **دقیقاً برابر اسلاگ مقاله** تا انتساب خودکار ممکن باشد.

### ۱۲.۴ متن جایگزین فارسی — الزامی

طبق راهنمای رسمی Google Search Central، متن جایگزین باید
**محتوای تصویر** را توصیف کند، نه عنوان صفحه را تکرار.

| قاعده | توضیح |
|---|---|
| توصیف تصویر | آنچه واقعاً دیده می‌شود، نه عنوان مقاله |
| مشخص باش | «ترازوی شواهد با یک کفهٔ خالی»، نه «تصویری دربارهٔ شبه‌علم» |
| بدون انباشت کلیدواژه | تکرار «کوانتوم» در هر جمله ممنوع |
| بدون پیشوند | با «تصویر…» یا «عکس…» شروع نشود |
| طول | ۱۰ تا ۱۸ واژه |
| زبان | **فارسی** |

نمونهٔ درست:

> «دو ابررسانا با لایهٔ عایق نازک و جفت‌های کوپر در حال تونل‌زنی،
> به‌همراه حلقهٔ اسکوئید»

نمونهٔ نادرست:

> «تصویر شاخص مقالهٔ پیوند جوزفسون — کوانتوم، ابررسانا، کیوبیت»

---

## بخش ۱۳ — اقلام آینده (ثبت‌شده، برای اجرا در وقتش)

**نوار اطلاعات نویسنده (طراحی مصوب ۱۱ شهریور — کد آماده در گیت):**
- یک نوار **آبی تیره** با تایپ **انگلیسی سبز مدل سایبری**، در پایان هر مقاله/دانشمند.
- در موبایل **هرگز دو خط نمی‌شود** (تک‌خط با nowrap؛ متن طولانی با سه‌نقطه کوتاه می‌شود).
- نام و اطلاعات **خودکار** از «نویسندهٔ برگه» (post_author) خوانده می‌شود.
- نقشهٔ سه نویسنده **تکمیل شد** (۱۱ شهریور) و **هیچ نام کاربری وردپرسی در کد نیست** — کلید نقشه «نام نمایشی» است (تطابق اغماضی؛ نویسندهٔ ناشناس → نوار نمایش داده نمی‌شود):
  - `Reza Darvishi` → Chemical Engineer, Petroleum University of Technology, Ahvaz — Independent Quantum Researcher
  - `Adel Lak` → Electrical & Electronics Engineer, Naghsh-e Jahan University, Isfahan — Independent Quantum Science Researcher
  - `M.R. Bardia` → Founder & Editor, QPedia — Graduate of Jundishapur University, Ahvaz
- فایل‌های پچ: `author-bar-patch/` در ریپو (CSS + PHP + درج قالب + README). اعمال روی سرور فقط با تأیید مالک.

---

## بخش ۱۴ — منابع لینک‌دار و تأییدشده (مصوب ۱۴ شهریور ۱۴۰۵ — الزامی)

> **قاعده:** بخش «منابع» بدون لینکِ واقعیِ تست‌شده، **نقص انتشار** است و مقاله برگشت می‌خورد.

### ۱۴.۱ — چه چیزی منبع معتبر است
| نوع منبع | شکل الزامی لینک |
|---|---|
| مقالهٔ علمی (Journal / Conference) | **فقط `https://doi.org/<DOI>`** — نه لینک ناشر، نه PDF، نه arXiv |
| صفحهٔ نوبل | لینک مستقیم صفحهٔ سال و رشته: `https://www.nobelprize.org/prizes/<field>/<year>/summary/` |
| نهاد علمی (NIST، CERN، NASA، NIH/NCCIH، IBM Quantum) | **لینک عمیق به همان صفحهٔ موضوعی**، نه صفحهٔ اصلی سازمان |
| دانشنامهٔ فلسفی (SEP) | لینک مستقیم مدخل: `https://plato.stanford.edu/entries/<slug>/` |
| مستند / رسانهٔ علمی | صفحهٔ رسمی ناشر (PBS، BBC)، نه آپلودهای شخصی |
| کتاب | فقط در کنار حداقل سه منبع لینک‌دار دیگر مجاز است |

### ۱۴.۲ — ممنوعیت‌های مطلق
- ❌ **نام دامنهٔ خالی** به‌جای لینک (`nobelprize.org`, `nature.com`).
- ❌ لینک به **صفحهٔ اصلی** سایت به‌جای صفحهٔ موضوعی (`https://www.imdb.com/`).
- ❌ لینک **بی‌ربط** به ادعای متن (منبعی که موضوعش چیز دیگری است).
- ❌ DOI **حدسی/بازسازی‌شده** بدون راستی‌آزمایی.
- ❌ ویکی‌پدیا به‌عنوان منبع اصلی.

### ۱۴.۳ — روش اجباری راستی‌آزمایی (قبل از تحویل)
۱. **پیدا کردن DOI** از Crossref:
   `https://api.crossref.org/works?rows=3&select=DOI,title,container-title,issued&query.bibliographic=<عنوان+نویسنده+مجله+سال>`
   اگر نتیجه بی‌ربط بود، دقیق‌تر: `&query.title=...&query.container-title=...`
۲. **تطبیق سه‌گانه:** عنوان + نام مجله + سال باید با متن مقاله یکی باشد. اگر سال فرق داشت، **متن مقاله اصلاح می‌شود، نه منبع.**
۳. **تأیید ثبت DOI:** `https://api.crossref.org/works/<DOI>/agency` باید `crossref` برگرداند.
۴. **تست لینک‌های غیر‌DOI:** صفحه واقعاً باز شود. اگر «404 / Page not found» بود، لینک **حذف و جایگزین** می‌شود — نه اینکه بماند.

### ۱۴.۴ — قالب HTML الزامی بخش منابع
```html
<h2>منابع</h2>
<ol>
<li>Zhang, J. et al. "Observation of a discrete time crystal", <em>Nature</em>, 543, 2017. DOI: <a href="https://doi.org/10.1038/nature21413" target="_blank" rel="noopener nofollow">10.1038/nature21413</a></li>
<li>NIST — پروژهٔ استانداردسازی رمزنگاری پساکوانتومی — <a href="https://csrc.nist.gov/projects/post-quantum-cryptography" target="_blank" rel="noopener nofollow">https://csrc.nist.gov/projects/post-quantum-cryptography</a></li>
</ol>
```
- ترتیب اجزا: نویسنده → «عنوان» → `<em>مجله</em>` → جلد → سال → `DOI:` → لینک.
- هر `<a>` خارجی **حتماً** با `target="_blank" rel="noopener nofollow"`.
- متن لینکِ DOI = خودِ DOI (نه «اینجا کلیک کنید»).
- تعداد: **۳ تا ۶ منبع**. منابع خارجی با شمارش لینک داخلی (بخش ۵) قاطی نمی‌شوند.

---

## بخش ۱۵ — نقل‌قول: قاعدهٔ «مضمون به‌جای نقل‌قول» (مصوب ۱۴ شهریور ۱۴۰۵ — الزامی)

> **قاعده:** به‌جای نقل‌قول مستقیم، **مضمون فکری مستند** نوشته می‌شود؛ مگر آنکه عین جمله در منبع مکتوبِ لینک‌دار قابل ردیابی باشد.

### ۱۵.۱ — چرا
فضای فارسی و انگلیسی پر از جملات ساختگی یا تحریف‌شدهٔ منسوب به فیزیک‌دانان بزرگ است — خصوصاً آن‌هایی که کوانتوم را به «آگاهی»، «قانون جذب» و «ارتعاش» وصل می‌کنند. یک نقل‌قول جعلی، اعتبار کل مقاله را از بین می‌برد.

### ۱۵.۲ — الگوی ممنوع در برابر الگوی درست
| ❌ ممنوع | ✅ درست |
|---|---|
| «شنیده‌ام یکی از بنیان‌گذاران کوانتوم می‌گفت…» | «نیلز بور بارها تأکید می‌کرد که… (بور، ۱۹۴۹)» + منبع لینک‌دار |
| «فیزیک‌دان برجسته‌ای گفت خدا تاس نمی‌ریزد» | توضیح موضع اینشتین + ارجاع به مقالهٔ EPR 1935 با DOI |
| «در روایات علمی آمده است که…» | نام شخص + سال + مقاله/کتاب قابل ردیابی |
| نقل‌قول با نام مبهم («استاد فیزیک سرشناسی») | یا نام کامل + منبع، یا کل جمله حذف شود |

### ۱۵.۳ — اجرای عملی
- هر ادعای منتسب به یک شخص = **نام کامل + سال + منبع لینک‌دار**.
- اگر منبع پیدا نشد → جمله حذف می‌شود، **بازنویسی مبهم جایگزینش نمی‌شود.**
- عبارت‌های تاریخیِ رایج (مثل «خفه شو و محاسبه کن») فقط با ذکر **انتساب واقعی** می‌آیند؛ اگر منبع رایج غلط است، همان اشتباه هم تصحیح می‌شود.
- در مقالاتی که موضوعشان خودِ نقل‌قول‌هاست، **هشدار صریح** دربارهٔ جعلی بودن نقل‌قول‌های اینترنتی در متن گنجانده می‌شود.
- تگ `<blockquote>` فقط برای متن دارای منبع؛ برای «روایت شنیده‌ای» هرگز.

---

## بخش ۱۶ — قاعدهٔ اصلاح ورودی خام (مصوب ۱۴ شهریور ۱۴۰۵)

وقتی متن خام (پیش‌نویس، خروجی ابزار، یا فایل ارسالی) با بخش‌های ۱۴ و ۱۵ در تضاد است:
۱. **ساختار خام تعیین‌کننده نیست.** اگر بدنهٔ خام بر پایهٔ نقل‌قول‌های بی‌منبع بنا شده، **زاویهٔ مقاله عوض می‌شود** (مثلاً «۱۰ نقل‌قول» → «۱۰ مضمون فکری مستند»).
۲. **لینک‌های خام تست می‌شوند.** هر URL موجود در متن خام قبل از انتقال باز می‌شود؛ ۴۰۴ها اصلاح یا حذف می‌شوند.
۳. **اجزای ناسازگار حذف می‌شوند:** خط «نویسنده/تاریخ انتشار»، تیتر `#`، جداکنندهٔ `---`، بلوک «چک‌لیست متادیتای انتشار»، ایموجی در تیترها.
۴. **تغییر زاویه در گزارش تحویل ذکر می‌شود** تا مالک بداند چه چیزی و چرا عوض شده.

---

---

## بخش ۱۷ — قاعدهٔ بازنگری‌شدهٔ لینک داخلی (مصوب ۱۵ شهریور ۱۴۰۵ — نسخه ۱.۴)

### ۱۷.۱ چرا سقف قبلی برداشته شد
سقف «۳ تا ۷ لینک داخلی» در نسخه‌های پیشین یک **قاعدهٔ محافظه‌کارانهٔ داخلی** بود،
نه الزام موتور جست‌وجو. گوگل سقف عددی مشخصی برای لینک داخلی اعلام نکرده است؛
راهنماهای رسمی فقط از «تعداد معقول» صحبت می‌کنند و آنچه جریمه می‌شود
**لینک‌سازی دستکاری‌گرانه** است، نه لینک مرتبط و مفید.

معیار درست، عدد نیست؛ **معنادار بودن** است. لینکی که خواننده را به توضیح واقعی
یک مفهوم می‌برد، ارزش افزوده دارد — چه پنجمین لینک باشد چه بیستمین.

### ۱۷.۲ قاعدهٔ جدید لینک موضوعی
* **سقف عددی سخت‌گیرانه‌ای وجود ندارد.** معیار: هر لینک باید به مفهومی برود که
  خواننده در همان لحظه ممکن است بخواهد بیشتر بداند.
* راهنمای عملی: مقالهٔ متعارف حدود **۵ تا ۱۰** لینک موضوعی؛
  **صفحهٔ محوری (pillar)** می‌تواند **۱۵ تا ۲۵** لینک داشته باشد و این کاملاً مجاز است.
* هر اسلاگ مقصد **فقط یک بار** در هر مقاله لینک می‌شود (اولین ذکر معنادار).
* لینک در متن جاری قرار می‌گیرد، نه در فهرست‌های تزئینی انتهای مقاله.
* لینک هرگز داخل تیتر و هرگز در بخش منابع نمی‌آید.

### ۱۷.۳ قاعدهٔ تازه: لینک اجباری دانشمندان
**هر جا نام دانشمندی می‌آید که صفحهٔ اختصاصی روی سایت دارد، باید لینک بخورد.**

* **یک بار در هر مقاله** — اولین ذکر نام. اگر نام سه بار تکرار شود، فقط بار اول لینک می‌شود.
* اگر پنج دانشمند در یک مقاله نام برده شوند، **هر پنج نفر** لینک می‌گیرند.
* **این لینک‌ها از هر سقفی مستثنا هستند** و جدا از لینک‌های موضوعی شمرده می‌شوند.
* مرجع نام‌ها و اسلاگ‌ها: ردیف‌های `scientist` در `فهرست-اسلاگ-ها.csv` (اکنون ۵۳ صفحه).
* اگر دانشمندی صفحه ندارد، **لینک ساخته نمی‌شود** و نامش ساده می‌ماند.

### ۱۷.۴ ابزار اجرا
اسکریپت `expand_links.py` در ریشهٔ مخزن این کار را خودکار می‌کند:

    python3 expand_links.py slug1 slug2 ...

نکات پیاده‌سازی که رعایت می‌کند:
* **مرز کلمهٔ فارسی** — با نگاه به عقب و جلو مانع تطبیق نام درون کلمهٔ دیگر می‌شود.
  (باگ واقعی مشاهده‌شده: «بور» درون «عبور» لینک می‌خورد.)
* **بازه‌های محافظت‌شده** — داخل تگ `<a>` موجود و کل بخش منابع دست‌نخورده می‌ماند.
* **نام‌های بلندتر اول** — تا «جان استوارت بل» پیش از «جان بل» تطبیق یابد.
* **بدون تکرار** — هر اسلاگ حداکثر یک بار.

### ۱۷.۵ آنچه همچنان ممنوع است
* لینک به صفحه‌ای که وجود ندارد (پیش از انتشار در برابر `فهرست-اسلاگ-ها.csv` بررسی شود).
* لینک تکراری به یک مقصد در یک مقاله.
* لینک با متن بی‌معنا مانند «اینجا کلیک کنید».
* انبوه‌سازی لینک بدون ارتباط موضوعی صرفاً برای افزایش تعداد.

---

*پایان دستورالعمل — پس از تأیید، این سند تنها مرجع الزام‌آور نوشتن مقالات QPedia است و نسخهٔ قبلی (`قالب-مقاله-نویسی-QPedia.md`) باطل اعلام می‌شود.*

---

## بخش ۱۸ — کف واژگان (مصوب ۱۵ شهریور ۱۴۰۵ — نسخه ۱.۵)

* **کف مطلق: ۷۵۰ واژه.** هیچ مقاله‌ای زیر این عدد منتشر یا رها نمی‌شود.
* **هدف متعارف: ۹۰۰ واژه یا بیشتر.**
* اگر مقاله‌ای کامل بود ولی زیر کف ماند، **حتماً اصلاح می‌شود** —
  نه با پرگویی، بلکه با افزودن بخشی که واقعاً چیزی به خواننده بدهد:
  یک نمونهٔ عددی، یک ماجرای تاریخی، یک سوءتفاهم رایج، یا کاربرد واقعی.

> قاعدهٔ سنجش: اگر بخش تازه را برداریم و مقاله چیزی از دست ندهد،
> آن بخش پرکننده بوده و باید بازنویسی شود.

---

## بخش ۱۹ — صدای نویسنده (مصوب ۱۵ شهریور ۱۴۰۵ — لغو قاعدهٔ پیشین)

### ۱۹.۱ چه چیزی لغو شد

در بازبینی‌های اولیه هر جملهٔ محاوره‌ای حذف می‌شد. **این قاعده لغو است.**

دلیل مالک: متنی که تمام صدای انسانی‌اش را از دست بدهد، هم برای
خوانندهٔ فارسی‌زبان سرد می‌شود و هم از نظر گوگل «فاقد اصالت» است.
راهنمای رسمی گوگل می‌گوید تمرکز بر **کیفیت** است نه نحوهٔ تولید؛
آنچه جریمه می‌شود *scaled content abuse* و بی‌اصالتی است.

### ۱۹.۲ قاعدهٔ جاری

| می‌ماند و افزوده می‌شود | بازنویسی می‌شود |
|---|---|
| خطاب مستقیم به خواننده | «بچه ها» |
| «راستش را بخواهید…» | «حواستون رو جمع کنید» |
| «به نظر من…» | «خوب یه جمع بندی کنیم» |
| «اجازه بدهید صادق باشم» | «بریم سر اصل مطلب» |
| «یک هشدار دوستانه بدهم» | لحن کلاس درس شفاهی |

* عبارت‌هایی که **اشکال نگارشی**‌اند بازنویسی می‌شوند، نه صدای نویسنده.
* اگر عبارتی بخشی از **تصویرسازی** باشد (مثلاً «بچه‌ها بدوند» در تشبیه
  یک صحنه)، دست نمی‌خورد — خطاب به خواننده نیست.
* جمله‌های نویسنده‌محورِ تازه باید **با هم متفاوت** باشند. اگر همه با
  یک عبارت شروع شوند، دقیقاً همان الگوی ماشینی ساخته می‌شود که
  می‌خواستیم از آن پرهیز کنیم.

---

## بخش ۲۰ — متادسکریپشن (مصوب ۱۵ شهریور ۱۴۰۵)

هر مقاله **باید** توضیح متا داشته باشد. اگر خالی بماند، گوگل خودش
تکه‌ای از متن را برمی‌دارد و معمولاً بدترین تکه را انتخاب می‌کند.

| قاعده | مقدار |
|---|---|
| طول | ۹۰ تا ۱۴۰ کاراکتر |
| ساختار | پرسش یا گزارهٔ روشن + دو تا سه نکتهٔ **مشخص** از خود مقاله |
| ممنوع | کلی‌گویی، تکرار صرف عنوان |
| تنوع | هیچ دو توضیحی با عبارت یکسان شروع نشود |

کلیدهای متا در این سایت: `rank_math_description` و
`_yoast_wpseo_metadesc` — **هر دو** نوشته می‌شوند، چون هر دو افزونه
روی سایت داده دارند.

---

## بخش ۲۱ — بازبینی مقالات موجود (مصوب ۱۵ شهریور ۱۴۰۵)

وقتی مقاله‌ای که **از قبل منتشر شده** بازبینی می‌شود:

### ۲۱.۱ اصل بنیادی
**هیچ واژه‌ای از متن اصلی حذف نمی‌شود.** فقط افزوده می‌شود.
تنها استثنا: عبارت‌های نگارشیِ نامناسب طبق بخش ۱۹.

### ۲۱.۲ تحویل
* از راه **افزونهٔ ایمپورتر**، با **دقیقاً همان اسلاگ** — تا فقط
  به‌روزرسانی شود، نه ساخت پست جدید.
* حداکثر **پنج مقاله در هر بسته** (مگر بسته‌های فقط-لینک).
* مقالاتی که مشکلی ندارند **دست نمی‌خورند**.

### ۲۱.۳ چهار محافظ ایمنی الزامی در هر افزونه
۱. اسلاگ ناموجود → رد شود؛ **هرگز پست جدید ساخته نشود**
۲. `post_date` و `post_status` حفظ شود (`edit_date=true`)
۳. متا فقط اگر خالی باشد نوشته شود
۴. دسته فقط اگر مقاله هیچ ترمی ندارد

### ۲۱.۴ اعتبارسنجی پیش از تحویل
* متن اصلی **زیرمجموعهٔ** خروجی باشد (تست حذف واژه)
* HTML معتبر · بدون لینک تودرتو · بدون خودلینک · بدون مقصد غایب
* بلوک منابع دقیقاً یک بار و در انتها
* هر مقاله دست‌کم **سه منبع اولیه** با DOI تأییدشدهٔ Crossref

> **هشدار ابزار:** هنگام مقایسهٔ متن، تگ‌ها را با **فاصله** جایگزین
> کنید نه رشتهٔ خالی؛ وگرنه واژه‌های دو طرف تگ به هم می‌چسبند و
> حذف کاذب گزارش می‌شود.

