# گزارش جامع وضعیت و تغییرات (CHANGELOG) — دانشنامه کیوپدیا (QPedia.ir)

> 📘 **مرجع اصلی پروژه از امروز:** `عصاره-کامل-پروژه-QPedia.md` (نمودار زمانی + معماری + رجیستری باگ‌ها + پیشرفت + نقشهٔ راه). این فایل لاگ تفصیلی رویدادهاست.

**تاریخ اسکن و تدوین:** ۶ شهریور ۱۴۰۴ / ۲۸ اوت ۲۰۲۶  
**مبنای استناد:** منحصراً دو منبع (۱. اسکن زنده سایت `https://qpedia.ir` / ۲. سورس کدهای قالب والد و فرزند در گیت‌هاب).

---

## ۱۴۰۵/۰۶/۱۴ — پاکسازی `functions.php` قالب فرزند (v1.0.3-lux)

منبع: زیپ محلی `quantum-pedia-child (6).zip` + `functions1.php.txt` در [lakanzino/qpedia.ir](https://github.com/lakanzino/qpedia.ir)

رفع‌ها:
- حذف چسبیدن دو فایل `functions.php` به هم و enqueue تکراری
- حذف نوتیس دیباگ `unfiltered_html` از پیشخوان
- خنثی‌سازی `fonts.css` والد با `wp_dequeue_style` (بدون deregister)
- ترجمهٔ رشته‌های انگلیسی هدر/جست‌وجو
- گارد `file_exists` برای ماژول glossary
- مستثنی کردن `quantum-sitemap.xml` و برگه‌های ثابت از قانون catch-all
- نرمال‌سازی جست‌وجوی فارسی روی خود کوئری، نه فقط نمایش

بستهٔ نصب: `quantum-pedia-child.zip`

---

## 🏆 دستاورد بزرگ: استقلال کامل از افزونه‌ها (Plugin-Independent)

با انتقال کدهای ساختاری به پوسته فرزند (`quantum-pedia-child`)، تمامی افزونه‌های سفارشی با موفقیت غیرفعال شدند و سایت هم‌اکنون تنها با **۲ افزونه خدماتی/سرعتی** با حداکثر توان و ثبات در حال کار است:
1. **LiteSpeed Cache** (بهینه‌سازی سرعت و کش سرور)
2. **UpdraftPlus** (پشتیبان‌گیری امن)

---

## ۱. نتایج اسکن زنده سایت پس از غیرفعال‌سازی افزونه‌ها (`https://qpedia.ir`)

| بخش | وضعیت HTTP | نتیجه تست واقعی زنده |
|---|:---:|---|
| **صفحه نخست (`/`)** | **200 OK** | کاملاً فعال؛ هدر اختصاصی، فوتر، هاب موضوعات، شمارنده‌ها، ویترین دانشمندان و آخرین مقالات بالا هستند. |
| **مقالات زنده (`quantum_article`)** | **200 OK** | **۳۴ مقاله منتشرشده** با چیدمان کامل، فهرست مطالب خودکار (TOC)، استایل‌های اختصاصی و مقالات مرتبط. |
| **دانشمندان زنده (`quantum_scientist`)** | **200 OK** | **۲۳ دانشمند منتشرشده** در تالار دانشمندان و صفحات اختصاصی با عکس و بیوگرافی کامل فعال هستند. |
| **دسته‌بندی‌ها (`quantum_category`)** | **200 OK** | **۱۲ رده فعال** (۶ دسته اصلی + ۶ زیردسته) در مسیرهای `/topic/{slug}/`. |
| **نقشه سایت (`/wp-sitemap.xml`)** | **200 OK** | فعال و معتبر؛ شامل ۴ زیرنقشه نیتیو (برگه‌ها، مقالات، دانشمندان، دسته‌ها). |
| **فایل خزنده‌ها (`/robots.txt`)** | **200 OK** | استاندارد و تمیز؛ نقشه ثبت شده و تاخیر خزش برقرار است. |
| **اندپوینت REST (`/wp-json/`)** | **200 OK** | پست‌تایپ‌ها و تاکسونومی‌ها مستقیماً از توابع پوسته فرزند سرویس داده می‌شوند. |

---

## ۲. وضعیت نهایی معماری پوسته فرزند (`quantum-pedia-child`)

پوسته فرزند اکنون مدیریت ۱۰۰٪ موارد زیر را بر عهده دارد:
* **ثبت CPTها و تاکسونومی‌ها:** مقالات کوانتوم (`quantum_article`) و دانشمندان (`quantum_scientist`) در `functions.php`.
* **بازنویسی مسیرها:** آدرس‌های تخت `/{slug}/` برای مقالات و `/scientists/{slug}/` برای دانشمندان.
* **رندر خودکار مقالات:** فهرست مطالب (TOC)، استایل‌ها، جعبه‌های نقل‌قول و مقالات مرتبط در `single-quantum_article.php`.
* **رندر تالار و مشخصات دانشمندان:** در `single-quantum_scientist.php` و `archive-quantum_scientist.php`.
* **رندر صفحه اصلی و فید دانشنامه:** در `front-page.php`.
* **ریدایرکت هوشمند:** هدایت مستقیم آدرس‌های قدیمی اصطلاحات به مقالات متناظر.
* **خنثی‌سازی شورت‌کدهای مرده:** بی‌اثرسازی `[qsci_card]`, `[qpt_card]`, `[qterm]` در سطح هسته پوسته.

---

## ۳. بسته تصاویر شاخص دانشمندان (`scientist-covers.zip`) — ۹ شهریور ۱۴۰۴ / ۳۰ اوت ۲۰۲۶

بسته جدید تصاویر شاخص برای هر **۲۳ دانشمند منتشرشده** طراحی و روی گیت‌هاب قرار گرفت:

* **فایل بسته:** `scientist-covers.zip` — شامل ۲۳ کاور JPG + فایل فهرست فارسی (`فهرست-فایلها.txt`).
* **ابعاد استاندارد:** تمام کاورها دقیقاً **۱۶:۹** (۱۳۷۶×۷۶۸).
* **قالب یکسان بصری:** چهره سیاه‌وسفید نزدیک به اصل در مرکز، بدون قاب سخت و محوشده در افکت‌های سایبری-کوانتومی اطراف (مدار ذره، موج تابع، مدار منطقی، نمادهای ψ و ħ).
* **هویت برند روی کاور:** حکاکی ظریف سایبری `QPEDIA` و `qpedia.ir` با خط نورانی افقی در بالای هر کاور.
* **نام‌گذاری فایل‌ها:** بر اساس slug واقعی هر دانشمند در سایت زنده (مثلاً `albert-einstein.jpg`) برای اتصال راحت از طریق «تصویر شاخص» در وردپرس.

**روش استفاده:** دانلود `scientist-covers.zip` → در هر صفحه دانشمند، تصویر شاخص را از رسانه وردپرس تنظیم کنید (نام فایل مطابق slug دانشمند است).

---

## ۴. اسکن زنده مقالات + درختچه + قالب مقاله‌نویسی — ۹ شهریور ۱۴۰۵ / ۳۱ اوت ۲۰۲۶

* اسکن REST سایت زنده: **۴۲ رکورد مقاله** (۴۱ یکتا) در ۱۴ دسته.
* **تکراریِ دقیق پیدا شد:** مقالهٔ ۵۱۶ کپی کامل ۵۱۱ است (`quantum-alternative-medicine-science-2`) — برای حذف/پیش‌نویس علامت خورد.
* دستهٔ `mathematics` خالی است و برای مقالات فرمالیسم آماده است.
* دو فایل جدید در ریپو: `درختچه-مقالات-و-دسته‌ها.md` و `قالب-مقاله-نویسی-QPedia.md`.

---

## ۵. دستورالعمل رسمی مقاله‌نویسی — تأییدشده — ۱۰ شهریور ۱۴۰۵ / ۳۱ اوت ۲۰۲۶

* سند الزام‌آور `دستورالعمل-مقاله-نویسی-QPedia.md` با تأیید مدیر پروژه نهایی شد (جایگزین `قالب-مقاله-نویسی-QPedia.md` که باطل اعلام شد).
* شامل: قانون صفر (سند زنده — هر قاعدهٔ جدید اول اینجا ثبت می‌شود)، قاعدهٔ طلایی «صبر کن، چی؟!»، ۱۲ اصل قبلی، جدول لحن، ۴ خط قرمز (متن خام AI، E-E-A-T، انتشار ستون‌به‌ستون، **تحویل فقط Markdown خالص بدون هیچ CSS**)، جدول سئو و چک‌لیست ۱۸ بندی.
* قاعدهٔ جدید ثبت‌شده: مقالات فقط به‌صورت Markdown خالص تحویل می‌شوند؛ هر تگ HTML/کلاس/استایل درون‌متنی ممنوع است چون قالب خودش CSS می‌گذارد و استایل دستی چیدمان را به هم می‌ریزد.

---

## ۶. بازنویسی سند فنی سیستم — v2.0.0 — ۹ شهریور ۱۴۰۵ / ۳۱ اوت ۲۰۲۶

* سند فنی قدیمی (v1.1.0، مبتنی بر معماری افزونه) کامل بازنویسی شد: `سند-فنی-و-وضعیت-سیستم.md` v2.0.0.
* حفظ شد: زیرساخت، ۴ تلهٔ حیاتی (زنجیرهٔ استایل، catch-all، ModSecurity، robots.txt)، چک‌لیست تغییر.
* حذف شد: تمام بخش‌های محتوایی/دسته‌بندی قدیمی (به فایل‌های درختچه منتقل شد).
* به‌روزرسانی شد: معماری پسا-افزونه (فقط LiteSpeed Cache + UpdraftPlus فعال)، قالب فرزند سبک v1.0.2-lux، آمار CPTها از اسکن زنده، وضعیت امنیتی با تست زندهٔ امروز.
* دو بدهی جدید ثبت شد: A1) ترجمهٔ ۲ رشتهٔ انگلیسی هدر/جست‌وجو در قالب فرزند، A2) خنثی‌سازی fonts.css والد با روش src-swap.

---

## ۸. یافتهٔ مهم: مشکل نمایش کدهای HTML در مقالات + توقف موقت پچ‌ها — ۱۰ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* **تشخیص زندهٔ مشکل انتشار مقالات (HTML خام دیده می‌شود):**
  - اسکن زندهٔ مقالهٔ ۵۱۸ (`spot-pseudoscience-one-sentence`) تأیید کرد **قالب فرزند سالم است**: H2، بولد، نقل‌قول، TOC، لینک داخلی و مقالات مرتبط همگی درست رندر می‌شوند.
  - قالب محتوا را با `wp_kses_post( do_shortcode( $content ) )` چاپ می‌کند — escape اضافه‌ای در قالب نیست.
  - **ریشهٔ فرضی مشکل:** نحوهٔ ورود متن به وردپرس — paste کردن HTML داخل بلاک گوتنبرگ باعث escape شدن تگ‌ها (نمایش `<h2>` به‌صورت متن خام) می‌شود؛ وردپرس Markdown را هم بومی نمی‌فهمد.
* **راه‌حل توافق‌شده (هنوز اعمال نشده):** پچ هدفمند به قالب فرزند — مبدل سبک Markdown→HTML داخل خود قالب (بدون افزونه، بدون CSS جدید)؛ استایل دقیقاً از CSS موجود قالب. مقالات فقط Markdown خالص.
* **تصمیم مالک — پچ‌های A1/A2 متوقف شد:** ترجمهٔ رشته‌ها و خنثی‌سازی fonts.css فعلاً روی هاست اعمال نمی‌شوند (نگرانی از شکستن سایت). نسخهٔ آماده در گیت‌هاب (کامیت `c6d7158`) نگه داشته می‌شود.
* **شرط شروع هر تغییر بعدی:** دریافت زیپ قالب فرزندِ فعلیِ روی سرور از مالک + آدرس یک مقالهٔ خراب برای بازبینی زنده. تا آن موقع هیچ فایلی تغییر داده نمی‌شود.
* **حالت کاری فعلی:** گیرنده — دریافت و ثبت فایل‌های مالک؛ تحلیل و دسته‌بندی بعد از کامل‌شدن ارسال.

---

## ۹. ثبت فهرست هدف رسمی پروژه (۱۶۰ مقاله + ۵۰ دانشمند + ۱۰۰ اسلاگ) — ۱۰ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* فهرست‌های هدف اعلامی مدیر پروژه دریافت و ثبت شد: `فهرست-هدف-و-پیشرفت-پروژه.md`.
* **پیشرفت مقالات (هدف ۱۶۰):** ✅ ۴۱ منتشرشده (۲۵٫۶٪) • 🆕 ۱۲ مستقیم + ۵ نسبی در صف ۲۰تایی • ⬜ ۱۰۲ باقی (۶۳٫۸٪). پوشش پس از موج ۲۰تایی: ~۳۳–۳۶٪.
* **پیشرفت دانشمندان (هدف ۵۰):** ✅ ۱۸ (۳۶٪) • ⬜ ۳۲ باقی. ۵ دانشمند منتشرشده خارج از فهرست: هیلبرت، دویچ، شور، ویلسون، کامپتون.
* عدد «۱۶۰» که قبلاً مالک اعلام کرده بود روشن شد: فهرست هدف است، نه آمار منتشرشده (سایت زنده ۴۲ رکورد دارد).
* ثبت: ۵ مقالهٔ در صف خارج از فهرست ۱۶۰ ولی داخل ۱۰۰ اسلاگ هستند. مورد «منحنی لیساژو» اسلاگ فارسی دارد (قبل از انتشار اصلاح شود).

---

## ۱۰. دریافت و ثبت استایل جدید بدنهٔ مقالات + نمونهٔ محتوا — ۱۰ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* دریافت از مالک: قطعه‌کد CSS برای `qpedia-layouts.css` (استایل‌های مدرن بدنهٔ مقاله: h2/h3 با خط نئونی، بلاک‌کوت، کادر `qp-callout` و `qp-callout--key`).
* اعمال شد روی **نسخهٔ گیت** فایل (نه هاست): به انتهای `quantum-pedia-child/assets/css/qpedia-layouts.css` اضافه شد (فایل از ۱۰۳۴ به ۱۰۵۹ خط رسید). زیپ قالب بازسازی شد (۲۳ فایل) و وجود استایل‌ها داخل زیپ تأیید شد.
* نمونهٔ محتوای مقالهٔ اعلامی مالک در `articles-samples/نمونه-محتوای-مقاله-qpedia.html` ثبت شد (HTML با کلاس‌های تم — بدون CSS درون‌متنی).
* ⚠️ نکتهٔ ثبت‌شده برای فاز تحلیل: رویکرد نمونهٔ جدید HTML است، در حالی که دستورالعمل مصوب قبلی Markdown خالص بود — جمع‌بندی نهایی در فاز تحلیل انجام می‌شود.
* ⚠️ فایل کامل اصلاح‌شده‌ای که مالک اشاره کرد ضمیمه نشد (uploads خالی بود) — از قطعه‌کد ارسالی استفاده شد. اگر فایل کامل فرق دارد، بعداً جایگزین می‌شود.
* هاست هنوز دست‌نخورده است؛ آپلود بعد از کامل‌شدن ارسال اطلاعات و تأیید نهایی.

---

## ۱۱. دریافت دستورالعمل رفع ایندکس‌نشدن مقالات در گوگل — ۱۰ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* مالک دستورالعملی **۳ بندی** برای رفع مشکل ایندکس‌نشدن مقالات اعلام کرد و **هر ۳ مورد دریافت و کامل شد**:
  ۱) بررسی تیک Noindex: تنظیمات ← خواندن ← «از موتورهای جستجو درخواست کن تا محتوای این سایت را بررسی نکنند» باید بدون تیک باشد.
  ۲) ثبت مستقیم سایت‌مپ اختصاصی CPT در Search Console: `wp-sitemap-posts-quantum_article-1.xml` علاوه بر `wp-sitemap.xml` + تست زندهٔ URL یکی از مقالات با URL Inspection.
  ۳) بررسی `robots.txt` (نبودن Disallow: /) و اطمینان از اینکه فایروال میزبان‌فا / لایت‌اسپید درخواست‌های Googlebot را بلاک نکرده باشد.
* توضیح مالک: مورد چهارمی وجود ندارد؛ دستورالعمل همان ۳ بند است.
* وضعیت: ثبت شد؛ اجرا و بررسی زندهٔ موارد در فاز تحلیل (بعد از کامل‌شدن ارسال اطلاعات).
* یادآوری فنی ثبت‌شده: `/wp-sitemap.xml` در اسکن‌های قبلی سالم بود و `robots.txt` هم Allow: / داشت؛ تست‌های زندهٔ مجدد هنگام اجرا انجام می‌شود.

---

## ۱۲. ثبت الزامات جدید پروژه — ۱۰ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* **سایت‌مپ اختصاصی مقالات:** ساخت/ثبت سایت‌مپ اختصاصی برای CPT مقالات به برنامهٔ بعدی اضافه شد (الزامی — حتماً انجام می‌شود). به‌همراه تست زندهٔ URL در Search Console.
* **بخش دانشمندان — دو الزام دائمی ثبت شد:**
  ۱) اعمال ۲۳ کاور ۱۶:۹ ساخته‌شده (`scientist-covers.zip`) به‌عنوان تصاویر شاخص دانشمندان.
  ۲) تعریف و ثبت «قالب/قاب اختصاصی دانشمندان» به‌عنوان تمپلت دائمی: هر بار زندگی‌نامهٔ دانشمند جدیدی ارسال شود، طبق همان تمپلت ساخته شود.
  📎 مرجع بصری این قالب، اسکرین‌شات مالک است (`Screenshot_2026-09-01-01-12-15-367_com.android.chrome.jpg`) — فایل در محیط کاری ننشست؛ هنگام اجرا، اسکرین‌شات دوباره ارسال یا جزئیات قاب‌بندی به‌صورت متن داده شود.
* **آمادگی دریافت فایل‌های قالب فرزند با پسوند txt:** مالک فایل‌های PHP قالب فرزند را با پسوند `txt` ارسال می‌کند (محدودیت آپلود چت)؛ ایجنت خودش پسوند را برمی‌گرداند و روی نسخهٔ گیت اعمال می‌کند.
* این دو الزام (سایت‌مپ + تمپلت دانشمندان) در بخش «کارهای باقی‌مانده» سند فنی هم ثبت شدند تا فراموش نشوند.

---

## ۱۳. وضعیت دریافت فایل‌های قالب فرزند (با پسوند txt) + تغییر نظر در تمپلت دانشمندان — ۱۰ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* مالک اسکرین‌شات قبلی را منسوخ اعلام کرد: قاب‌بندی قبلی (عکس مربعی بزرگ + عکس کوچک در کادر معرفی) دیگر مطلوب نیست؛ تمپلت نهایی دانشمندان بعداً با توضیح جدید تعریف می‌شود. (ثبت در سند فنی B6 به‌روز شد.)
* مالک ۱۰ فایل قالب فرزند را با پسوند `txt` ضمیمه کرد: custom1.js، qpedia-layouts1.css، custom1.css، taxonomy-quantum_category1.php، style1.css، single1.php، single-quantum_scientist1.php، single-quantum_article1.php، searchform1.php، header1.php.
* ⚠️ **فایل‌ها به محیط کاری نرسیدند** (پوشهٔ uploads خالی است — این مشکل در کل این جلسه تکرار شده). راه قطعی: زیپ این ۱۰ فایل را مستقیم در گیت‌هاب آپلود کن (مثل زیپ‌های قبلی قالب) و نامش را بگو — ایجنت پسوندها را اصلاح و نسخهٔ سرور را بازسازی می‌کند.
* یادآوری روال ثبت‌شده: پس از رسیدن فایل‌ها، اول تفاوت با نسخهٔ گیت گزارش می‌شود، بعد اعمال.

---

## ۱۴. دریافت کامل نسخهٔ سرور قالب فرزند + دسترسی به ۱۶۰ تصویر شاخص — ۱۰ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* **۱۵ فایل قالب فرزند (نسخهٔ واقعی سرور)** آپلودشدهٔ مالک در گیت‌هاب، در پوشهٔ جدید `server-child-theme/` سازماندهی شد (همراه `error_log` سرور):
  - PHP: functions1، header1، footer1، front-page1، single1، single-quantum_article1، single-quantum_scientist1، archive-quantum_article1، archive-quantum_scientist1، taxonomy-quantum_category1، searchform1
  - CSS/JS: custom1.css، qpedia-layouts1.css، style1.css، custom1.js
  - (نام یک فایل شامل فاصلهٔ اشتباه بود — اصلاح شد)
* **یافتهٔ اولیهٔ مهم (تحلیل کامل در فاز بعد):** نسخهٔ سرور جدیدتر از نسخهٔ گیت است — `QPEDIA_CHILD_VERSION = 2026.08.30-ui3` (گیت: 2026.08.26-ui2). هدر فایل از «اصلاح‌شده ۱۴۰۴/۰۶/۰۹» می‌گوید: رفع باگ ۴۰۴ برگه‌ها، افزودن load_child_theme_textdomain، گارد CONCATENATE_SCRIPTS.
* **دسترسی به ۱۶۰ تصویر شاخص تأیید شد:** PR#1 مالک در `main` مرج شد — پوشهٔ `images/` شامل ۱۶۰ PNG مربعی (q_01 تا q_160، هرکدام ~۲MB) + فایل `quantum_image_prompts.md` با نگاشت هر تصویر به مقالهٔ هدف. این‌ها با فهرست ۱۶۰ مقالهٔ هدف هم‌شماره‌اند.
* ⚠️ دو پرچم برای فاز تحلیل: ۱) `error_log` (۲۹۳KB) لاگ سرور است و در ریپوی عمومی دیده می‌شود — تصمیم حذف با مالک. ۲) حجم تصاویر مجموعاً ~۳۳۰MB است — نحوهٔ انتقال به رسانه وردپرس (آپلود مستقیم، نه گیت) برنامه‌ریزی شود.
* ⚠️ هاست همچنان دست‌نخورده است؛ هیچ‌کدام از این فایل‌ها روی سایت اعمال نشده.

---

## ۱۵. بروزرسانی دستورالعمل مقاله‌نویسی — قواعد جدید مصوب مالک — ۱۰ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* **جامعهٔ محتوایی جدید ثبت شد:** ۸۴ مقالهٔ مبتنی بر جست‌وجوهای واقعی کاربران ایرانی در گوگل — همه مهم‌اند به‌جز تکراری‌های کامل؛ منبع موج‌های آتی بازنویسی.
* **بخش ۹ جدید (لحن معلمانهٔ فارسی):** فهرست اصطلاحات مصوب («بچه‌ها توجه کنید اینجا…»، «بریم سر اصل داستان…»، «ببینید بچه‌ها، می‌دونم کمی پیچیده به نظر میاد…» و…) — استفادهٔ خلاقانه، **هر عبارت حداکثر یک بار در هر مقاله**.
* **مثال‌ها الزاماً ایرانی:** شهر/استان/مکان‌های واقعی و فعلی ایران + نام‌های اصیل ایرانی.
* **بخش ۱۰ (نقل‌قول/فرمول/موضع):** نقل‌قول فقط معتبر و جذاب در Blockquote (جعلی ممنوع)؛ هر فرمول با «این فرمول فقط برای اینکه بدونید چه شکلیه، و وارد شکل تخصصی نمی‌شیم»؛ موضع ثابت = درک درست، ضدخرافه، «علم اثبات‌شده ولی حقیقت کاملش هنوز ناشناخته».
* **بخش ۱۱ (بستهٔ تحویل):** انتهای هر مقاله: پرسش‌وپاسخ پایانی (۳–۴) + چکیده + اسلاگ، جدا و آمادهٔ انتشار.
* **بخش ۱۲ (تصویر شاخص):** از این به بعد هر مقاله با تصویر شاخص **WebP بهینه‌شدهٔ همنام مقاله** تحویل می‌شود (مبنا: ۱۶۰ تصویر مربعی موجود؛ ابزار تبدیل در محیط تأیید شد — Pillow 12.3.0).
* **بخش 13 (آینده):** نوار باریک و ظریف اطلاعات نویسنده در پایان مقالات — کاملاً انگلیسی، مدرک تحصیلی + دانشگاه (اجرا بعداً با جزئیات مالک).
* چک‌لیست سند از ۱۸ به **۲۶ بند** رسید. عصارهٔ پروژه (بخش اصول نوشتاری) هم هم‌راستا شد.

---

## ۱۶. دریافت ۸۴ مقالهٔ جست‌وجومحور + کد نوار نویسنده — ۱۱ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* مالک ۸۶ فایل MD را مستقیم در گیت‌هاب آپلود کرد (کامیت `e06480a` روی `main`)؛ همه در پوشهٔ `articles-84/` برنچ کاری سازماندهی شدند.
* **۸۳ مقالهٔ یکتا** — ۳ زوج تکراری داخلی با پسوند `(1)` شناسایی شد (`رابطه فروپاشی…`، `گربه شرودینگر…`، `فتوسنتز…` که همتای آخرش در سایت منتشر شده). فهرست کامل عناوین در `articles-84/فهرست-عنوان‌ها.md`.
* **کد نوار نویسندهٔ پایان مقاله نوشته و در گیت ثبت شد** (طراحی مصوب مالک):
  - CSS در انتهای `qpedia-layouts.css`: نوار آبی تیره (#081431→#0c2247)، نام سبز سایبری #00e676، تایپ انگلیسی uppercase با letter-spacing؛ `flex-wrap: nowrap + white-space: nowrap + text-overflow: ellipsis` ← **در موبایل هرگز دو خط نمی‌شود**.
  - تابع `qpedia_author_bar()` + نقشهٔ `qpedia_get_author_credentials()` به `functions.php` اضافه شد — نام/اطلاعات **خودکار از نویسندهٔ برگه** خوانده می‌شود.
  - درج در `single-quantum_article.php` و `single-quantum_scientist.php` بعد از محتوا.
  - پوشهٔ `author-bar-patch/` با README برای اعمال هدفمند روی نسخهٔ سرور (ui3) ساخته شد.
* ⚠️ **در انتظار مالک:** اطلاعات سه نویسنده (نام انگلیسی + مدرک + دانشگاه) و نام کاربری وردپرس هر کدام برای تکمیل نقشه.
* ⚠️ سرور هنوز دست‌نخورده است؛ اعمال نوار فقط با تأیید مالک و به‌صورت پچ هدفمند.

---

## ۱۷. تکمیل نقشهٔ نویسندگان نوار پایانی — ۱۱ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* مالک اطلاعات سه نویسنده را داد؛ به انگلیسی استاندارد و مختصر تبدیل و در نقشه درج شد — **بدون هیچ نام کاربری** (خواستهٔ مالک):
  - `Reza Darvishi` → Chemical Engineer, Petroleum University of Technology, Ahvaz — Independent Quantum Researcher
  - `Adel Lak` → Electrical & Electronics Engineer, Naghsh-e Jahan University, Isfahan — Independent Quantum Science Researcher
  - `M.R. Bardia` → Founder & Editor, QPedia — Graduate of Jundishapur University, Ahvaz
* سازوکار نقشه از `user_login` به **«نام نمایشی» (display_name)** تغییر کرد؛ تطابق اغماضی (حروف کوچک + حذف نقطه/فاصله) — پس `M.R.Bardia` و `M.R. Bardia` هر دو شناخته می‌شوند. نویسندهٔ ناشناس → نوار عمداً مخفی (محافظ بی‌صدا).
* تابع کمکی `qpedia_normalize_author_name()` اضافه شد؛ قطعهٔ پچ `author-bar-patch/author-bar-functions.php.txt` از نسخهٔ گیت بازتولید شد و README پچ (نام نمایشی + تست سه نویسنده) به‌روز شد.
* دیپلم دبیرستان‌ها عمداً در نوار نیامد (مختصر و استاندارد؛ فقط مدرک دانشگاهی/نقش). لینکدین هم در نوار نیست (طراحی مصوب لینک ندارد).

---

## ۱۸. پایلوت پردازش ۸۴ مقاله: «تله‌پورت کوانتومی» — ۱۱ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* اولین مقالهٔ کورپوس با قالب کامل جدید بازنویسی شد (نمونه برای تأیید مالک قبل از پردازش انبوه): `articles-84/آماده/تله‌پورت-کوانتومی-چیست-و-چیست-نیست.md`.
* منبع ۱۳۶ کلمه‌ایِ خام (با خطاهای انگلیسی) → بازنویسی کامل ۱۳۷۳ کلمه: قلاب «ذره نابود می‌شود» از دل متن منبع + قیاس نامهٔ جوهر محوشونده (تهران→تبریز) + مثال ایرانی (میدان آزادی → نقش جهان، آرش و بیتا) + ۵ عبارت معلمانه (هرکدام یک‌بار) + نقل‌قول معتبر شرودینگر ۱۹۳۵ + بلوک‌کوت اصلاح سوءتفاهم FTL + ۵ لینک داخلی فقط به مقالات منتشرشده + ۳ مرجع واقعی (PRL 1993 / Nature 1997 / Nature 2017) + پرسش‌وپاسخ ۴تایی + چکیدهٔ ۱۴۷ کاراکتری + اسلاگ `quantum-teleportation`.
* تصویر شاخص WebP بهینه: `آماده/quantum-teleportation.webp` (از `images/q_127_teleportation.png`، ۲ مگابایت PNG → ۱۲۰ کیلوبایت WebP).
* «وضعیت پردازش» به `articles-84/فهرست-عنوان‌ها.md` اضافه شد (۱ از ۸۳).

---

## ۱۹. قواعد جدید تحویل + حذف تکراری‌ها + مقالهٔ ۲ — ۱۱ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* **قواعد جدید ثبت شد** (دستورالعمل بخش ۱۱ و بند ۹): پردازش مقاله‌به‌مقاله با تأیید مالک؛ تحویل در سه بخش جدا (متن برای کپی در چت + تصویر برای دانلود + بلوک مشخصات: تایتل/دسته/زیردسته/اسلاگ/چکیده)؛ طول مقاله «راهنما» شد نه شرط سخت (کیفیت، مفهوم، مثال و پرسش مهم‌تر).
* **قاعدهٔ متای گوگل:** چکیده در فیلد Excerpt وردپرس می‌رود، نه داخل بدنه — سرور از قبل `<meta name="description">` + OpenGraph + JSON-LD را خودکار از همان فیلد می‌سازد (`qpedia_auto_seo_meta_tags`، L465-508).
* **تکراری‌ها حذف شد:** `رابطه فروپاشی (1)` (عیناً تکراری)، `گربه شرودینگر` نسخهٔ کوتاه (نسخهٔ کامل (1) ماند)، `فتوسنتز (1)` (عین مقالهٔ منتشرشدهٔ `quantum-photosynthesis`). کورپوس: ۸۲ مقالهٔ یکتا.
* **مقالهٔ ۲ پردازش شد:** «آیا با فکر کردن می‌توان واقعیت کوانتومی را تغییر داد؟» — قلاب «دستگاه بی‌ذهن همان کاری را می‌کند که می‌گویند ذهن می‌کند» + قیاس دستگاه خودکار متروی تهران + سکهٔ شیر‌یا‌خط + نقل‌قول معتبر فاینمن ۱۹۶۵ + تاریخچهٔ گمانهٔ ویگنر ۱۹۶۱ + ۶ لینک داخلی + پرسش‌وپاسخ ۴تایی + چکیدهٔ ۱۵۰ کاراکتری + اسلاگ `mind-quantum-reality` + تصویر `mind-quantum-reality.webp` (از `q_155_consciousness.png`).

---

## ۲۰. فرمت تحویل نهایی شد: باکس کپی — ۱۱ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* مالک فرمت تحویل را قطعی کرد: متن مقاله فقط داخل یک «باکس کپی» (کدبلاک) در چت، بدون تیتر H1 و بدون بلوک بستهٔ انتشار؛ مشخصات (تایتل/دسته/زیردسته/اسلاگ/چکیده) بیرون باکس؛ تصویر از نمایشگر فایل. قاعده در دستورالعمل بخش ۱۱ ثبت شد.

---

## ۲۱. نسخه‌های کامل فایل سرور برای نوار نویسنده — ۱۱ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به‌درخواست مالک، نسخهٔ **کامل** چهار فایل (پایهٔ ui3 واقعی سرور + نوار نویسنده) در `author-bar-full-files/` ساخته شد: `functions.php`، `single-quantum_article.php`، `single-quantum_scientist.php`، `qpedia-layouts.css`. روش تحویل: باکس کپی در چت، جای‌گذاری کامل روی سرور.

---

## ۲۲. اصلاح نوار نویسنده بعد از تست زنده — ۱۱ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* مالک نوار را روی سرور اعمال کرد و تأیید شد که کار می‌کند. دو اصلاح پس از بازخورد:
  - مدرک/دانشگاه عادل لک کوتاه شد تا در دسکتاپ کامل دیده شود («Electrical & Electronics Engineer, Naghsh-e Jahan University, Isfahan — Quantum Researcher»).
  - CSS: در دسکتاپ اگر متن جا نشود نوار به خط بعد می‌رود (متن کامل دیده می‌شود)؛ در موبایل همچنان تک‌خط با سه‌نقطه (قانون قبلی حفظ شد).
  - راهنمای «نام نمایشی admin»: نوار بر اساس display_name است نه نام کاربری — مالک باید نمایش نام خود را `M.R. Bardia` کند.

---

## ۲۳. فرمت تحویل از Markdown به HTML ساده + اصلاح موبایل نوار — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* تست انتشار واقعی نشان داد وردپرس Markdown را تبدیل نمی‌کند (متن سادهٔ پشت‌سرهم منتشر شده بود). قانون تحویل عوض شد: **بدنهٔ مقاله HTML سادهٔ آماده** (فقط h2/h3/p/strong/em/blockquote/ul/ol/li/a) — دستورالعمل در ۵ نقطه به‌روز شد.
* نسخهٔ HTML هر دو مقالهٔ منتشرشده ساخته شد: `آماده/quantum-teleportation.html` و `آماده/mind-quantum-reality.html` (برای جایگزینی محتوای برگه‌ها).
* موبایل نوار نویسنده: قانون تکلاین برداشته شد → نوار در موبایل هم می‌تواند دو خط کوتاه شود و متن کامل دیده شود (فونت ریزتر، بدون سه‌نقطه).

---

## ۲۴. اصلاح فاصله‌گذاری کلمات (حذف نیم‌فاصله) — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* تست انتشار دوم: نوار نویسنده در موبایل درست شد، اما مقاله‌ها با کلمات چسبیده منتشر شدند. علت: نیم‌فاصله (ZWNJ) در مسیر کپی از چت به وردپرس حذف می‌شود و «تله پورت» → «تلهپورت» چسبیده رندر می‌شود.
* قانون جدید تحویل: **فاصلهٔ کامل بین اجزای کلمات، نیم‌فاصله ممنوع** — در دستورالعمل §۱۱ و چک‌لیست ثبت شد.
* هر دو مقاله (HTML و MD) از نیم‌فاصله پاک شدند (۲۳۲ و ۱۷۱ و ۲۴۲ و ۱۸۱ مورد) و با فاصلهٔ کامل بازسازی شدند.
* پاسخ به پرسش مالک: نام فرمتی که جواب داد = **HTML ساده** (همان باکس آخر) — مارک‌داون نیست؛ HTML خام فقط اگر داخل ویرایشگر بصری چسبانده شود خراب می‌شود، در تب «متن» درست رندر می‌شود.

---

## ۲۵. ترتیب نهایی بخش‌ها + سوالات متداول آکاردئونی — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک، ترتیب بخش‌های مقاله ثابت شد: **جمع‌بندی ← سوالات متداول ← منابع (آخرین بخش)**.
* «پرسش و پاسخ پایانی» به «سوالات متداول» تغییر نام داد و **آکاردئونی** شد: تگ استاندارد `details/summary` — بدون هیچ جاوااسکریپت، با لمس باز/بسته می‌شود، در وردپرس و همهٔ مرورگرها کار می‌کند.
* هر دو مقاله بازسازی شدند (`quantum-teleportation.html` و `mind-quantum-reality.html`): هر کدام ۴ سوال آکاردئونی، منابع در انتها.
* دستورالعمل §۱۱ و چک‌لیست با ترتیب ثابت و تگ‌های مجاز جدید (details/summary) به‌روز شدند.

---

## ۲۶. مقالهٔ ۳ از ۸۲: آزمایش‌های بل — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* **آزمایش‌های بل: چگونه درهم‌تنیدگی اثبات شد** پردازش و تحویل شد (اسلاگ `bell-experiments`، دسته: تاریخ و آزمایش‌های کوانتوم ← آزمایش‌های کوانتومی).
* قالب نهایی رعایت شد: HTML ساده، فاصلهٔ کامل (صفر نیم‌فاصله)، جمع‌بندی ← سوالات متداول آکاردئونی (details/summary) ← منابع در آخر.
* ۱۵۸۲ کلمه، ۶ لینک داخلی به مقالات منتشرشده (درهم‌تنیدگی، اندازه‌گیری، نامساوی بل، اسپین، رمزنگاری، واهم‌دوسی)، نقل‌قول‌های تأییدشده (EPR ۱۹۳۵ و «کنش شبح‌وار» با سند نامهٔ بورن)، اگزسرپت ۱۵۵ نویسه.
* خروجی: `آماده/آزمایش-های-بل-چگونه-درهم-تنیدگی-اثبات-شد.md` + `bell-experiments.html` + `bell-experiments.webp` (۹۷ KB از `q_35_bellinequality.png`).

---

## ۲۷. تصاویر شاخص واترمارک‌دار — نمونهٔ «کوانتوم یعنی چه؟» — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک، تصویر شاخص همهٔ مقالات منتشرشده باید WebP استاندارد (۱۲۰۰×۶۷۵ = سایز توصیه‌شدهٔ وردپرس/او‌جی)، سبک و باکیفیت، کراپ مرکزی با حفظ safe zone، و **واترمارک qpedia.ir** داشته باشد: نوار سبز بسیار باریک، تمام‌عرض، در ناحیهٔ یک‌سوم تا مرکز ارتفاع، متن مشکی qpedia.ir با فاصلهٔ حروف باز.
* ابزار استاندارد ساخته شد: `tools/make_featured.py` (ورودی PNG آرشیو ۱۶۰تایی → خروجی WebP واترمارک‌دار).
* نمونهٔ اول تولید و ارائه شد: `images-featured-webp/what-is-quantum.webp` (از `images/q_01_quantum.png`، ۷۶ کیلوبایت).
* در انتظار تأیید مالک؛ بعد از تأیید، هر ۴۱ مقالهٔ منتشرشده به‌صورت خودکار تولید می‌شوند.

---

## ۲۸. واترمارک نسخهٔ ۲ — طرح مالک، وسط‌چین روی تصاویر زمینه‌دارک — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* مالک طرح نوار سبز را رد کرد و واترمارک خودش (خروجی Picsart) را برای تصاویر شاخص زمینه‌دارک فرستاد: **وسط‌چین، بالای تصویر**.
* فایل Picsart به سندباکس نرسید (پیوست‌های چت هیچ‌وقت به محیط دستیار نمی‌رسند — مسیر قطعی فقط آپلود در ریپوی گیت‌هاب). نمونهٔ جایگذاری با واترمارک متنی سفید موقت ساخته شد تا مالک جایگاه و اندازه را ببیند؛ به محض آپلود فایل واقعی، فقط همان فایل جایگزین می‌شود.
* `tools/make_featured.py` به نسخهٔ ۲ ارتقا یافت: کراپ ۱۲۰۰×۶۷۵ + چسباندن واترمارک PNG (آلفا، ~۳۴٪ عرض، حاشیهٔ ۴۸ پیکسل از بالا، وسط‌چین) + حالت متنی موقت.
* نمونه: `images-featured-webp/what-is-quantum.webp` (از `q_01_quantum.png`، درخشندگی ۲۹ = زمینهٔ دارک، ۷۹ کیلوبایت).

---

## ۲۹. واترمارک واقعی مالک دریافت و اعمال شد — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* فایل `Picsart_26-09-01_06-25-25-218.png` از `origin/main` دریافت شد (کامیت `42bde75`): **۸۰۰×۲۰۴ با شفافیت، رنگ سیان روشن** — مناسب زمینه‌های دارک.
* نسخهٔ تمیز در مخزن ذخیره شد: `tools/watermark.png`.
* نمونهٔ «کوانتوم چیست؟» با واترمارک واقعی بازسازی شد: `images-featured-webp/what-is-quantum.webp` (۱۲۰۰×۶۷۵، ۷۹ کیلوبایت، واترمارک وسط‌چین بالای تصویر با حاشیهٔ ۴۸ پیکسل و عرض ~۳۴٪).
* در انتظار تأیید مالک برای اجرای دسته‌ای روی همهٔ تصاویر شاخص زمینه‌دارک.

---

## ۳۰. پنل شیشه‌ای زیر واترمارک — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک، زیر واترمارک یک **جعبهٔ شیشه‌ای باریک هم‌اندازهٔ خود واترمارک** اضافه شد: پس‌زمینهٔ همان ناحیه محو می‌شود و یک گرادیان تیره (روشن بالا → تیره پایین) رویش می‌نشیند تا واترمارک روی زمینه‌های روشن هم مستقل و خوانا باشد؛ لبهٔ شیشه‌ای ظریف هم دارد.
* `tools/make_featured.py` نسخهٔ ۳ (glass panel با blur + گرادیان + لبه).
* نمونهٔ «کوانتوم چیست؟» بازسازی شد: `images-featured-webp/what-is-quantum.webp` (۱۲۰۰×۶۷۵، ۷۲ کیلوبایت).

---

## ۳۱. واترمارک نصف اندازه + تیرگی بیشتر پنل — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک: واترمارک **دو برابر کوچکتر** شد (عرض ~۱۷٪ تصویر به جای ~۳۴٪) تا فضای کمتری از کادر بگیرد؛ طرح و رنگ و شکل همان ماند.
* شدت تیرگی گرادیان شیشه کمی بیشتر شد (آلفای ۸۰→۱۸۵ به سمت پایین به جای ۶۰→۱۵۰).
* نمونهٔ «کوانتوم چیست؟» بازسازی شد: `images-featured-webp/what-is-quantum.webp` (۱۲۰۰×۶۷۵، ۷۶ کیلوبایت).

---

## ۳۲. واترمارک اصلاح‌شدهٔ مالک + شیشهٔ تقریباً تیره — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* مالک نسخهٔ اصلاح‌شدهٔ واترمارک را مستقیماً روی شاخهٔ کار آپلود کرد: `watermark-qpedia.ir.png` (کامیت `92669e5`) — ۳۶۵×۸۷، شفاف، سیان روشن. در `tools/watermark.png` ثبت شد (فایل پیشین Picsart جایگزین شد).
* گرادیان شیشه «تقریباً تیره» شد (آلفای ۱۵۰→۲۴۰ به سمت پایین)؛ جایگاه و اندازهٔ واترمارک همان تأییدشده ماند: بالا، وسط‌چین، ~۱۷٪ عرض.
* نمونهٔ «کوانتوم چیست؟» بازسازی شد: `images-featured-webp/what-is-quantum.webp` (۱۲۰۰×۶۷۵، ۷۶ کیلوبایت).

---

## ۳۳. تولید انبوه تصاویر شاخص واترمارک‌دار — ۴۴ تصویر — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* بعد از تأیید نهایی مالک، **۴۴ تصویر شاخص** با استاندارد نهایی ساخته شد: ۴۱ مقالهٔ منتشرشده + ۳ مقالهٔ جدید (تله‌پورت، فکر کردن، آزمایش‌های بل).
* همه: ۱۲۰۰×۶۷۵، WebP کیفیت ۸۲، کراپ مرکزی، واترمارک `watermark-qpedia.ir.png` وسط‌چین بالای تصویر (~۱۷٪ عرض) + پنل شیشه‌ای تقریباً تیره (محو + گرادیان تیره به سمت پایین).
* نگاشت اسلاگ→تصویر آرشیو در `tools/build_featured_batch.py` و جدول کامل (با درخشندگی و حجم) در `images-featured-webp/README.md`.
* خروجی‌ها: `images-featured-webp/` (۴.۳ مگابایت، ۴۴ فایل) + `images-featured-webp.zip` (۳.۷ مگابایت برای دانلود یکجا) + `preview-contact-sheet.jpg` برای بازبینی سریع.
* نکته: `bell-inequality` به `q_42_bellhidden` اختصاص یافت تا با `bell-experiments` (q_35) تداخل تصویری نداشته باشد.

---

## ۳۴. مقالات ۴ تا ۱۰ (۷ مقاله) پردازش و تحویل شد — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک، هفت مقالهٔ بعدی فهرست یکجا پردازش شد (۱۰ از ۸۲):
  `entanglement-myths`، `is-classical-physics-wrong`، `quantum-interpretation-debate`، `brain-quantum-phenomena`، `does-ai-use-quantum`، `does-quantum-prove-god`، `is-many-worlds-real`.
* سه اسلاگ اول مطابق درختچهٔ هدف (از قبل ردیف داشتند)؛ چهار ردیف جدید به درختچه اضافه شد.
* قالب نهایی در همه: HTML ساده، فاصلهٔ کامل (صفر نیم‌فاصله)، جمع‌بندی ← سوالات متداول آکاردئونی (details/summary) ← منابع آخر. هر کدام ۴ سوال، ۳-۷ لینک داخلی به اسلاگ‌های منتشرشده، اگزسرپت ۱۴۴-۱۶۶ نویسه.
* نقل‌قول‌های تأییدشده: نیوتن به هوک ۱۶۷۵ (مقالهٔ ۵)؛ بقیه بلاک‌کویت سوءتفاهم. حقایق سنددار: محاسبهٔ تگمارک ۲۰۰۰ (۱۰^-13 ثانیه) در مقالهٔ مغز، تراشهٔ Willow گوگل ۲۰۲۴ در مقالهٔ AI.
* `tools/md2html.py` به عنوان مبدل دائمی خانه ثبت شد (برای ۷۲ مقالهٔ باقی‌مانده).
* ۷ تصویر شاخص واترمارک‌دار جدید در `images-featured-webp/` (از q_34_epr، q_33_bohreinstein، q_149_measurementproblem، q_134_qbiology، q_117_computing، q_159_boundary، q_43_everett).
* نکتهٔ مالک: مقالهٔ مغز (brain-quantum-phenomena) زاویهٔ «بررسی ادعاها» دارد و مکمل `is-the-brain-quantum` است؛ در صورت تمایل حذف از لیست.

---

## ۳۵. مقالات ۱۱ تا ۱۷ — ۵ تحویل + ۲ حذف کپی — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک، دستهٔ ۱۱ تا ۱۷ پردازش شد:
  - تحویل: `quantum-career-future-learn`، `heisenberg-uncertainty-principle` (اسلاگ مطابق درختچه)، `einstein-photoelectric-effect` (تاریخ ← تاریخ)، `superposition-explained`، `why-large-objects-dont-superpose`.
  - **حذف طبق سیاست «تکراری هارا حذف کن»:** `اصل طرد پاولی` = کپی کامل مقالهٔ منتشرشدهٔ `pauli-exclusion-principle`؛ `الکترون` = کپی کامل `electron` (هر دو با fetch زندهٔ سایت تطبیق و تأیید شدند). شمارنده: **۱۵ از ۸۰**.
* سه مقالهٔ هم‌موضوع با مقالات زنده عمداً زاویهٔ مکمل گرفتند: فوتوالکتریک ← داستان اینشتین/سال معجزهٔ ۱۹۰۵/نوبل ۱۹۲۱؛ برهم‌نهی ← هوک آکورد موسیقی و خط قرمزهای جملهٔ معروف؛ اشیای بزرگ ← مرز کوانتوم/کلاسیک + استثناهای ابرشارگی و ابررسانایی.
* واقعیت‌های سنددار: نوبل ۱۹۲۱ با متن رسمی اعلامیه، طول موج دوبروی توپ تنیس (۱۰^-34 متر)، مولکول ۲۰۰۰ اتمی در برهم‌نهی (Nature Physics 2019)، هایزنبرگ ۱۹۲۷.
* ۵ تصویر شاخص جدید: q_122_willow، q_05_uncertainty، q_29_einstein، q_11_quantumstate، q_136_kitchen.

---

## ۳۶. مقالات ۱۸ تا ۳۰ — ۵ تحویل + ۸ حذف مشابه/تکراری — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک، دستهٔ ۱۸ تا ۳۰ پردازش شد (۱۳ فایل، پس از بررسی هر ۱۳ منبع):
  - تحویل: `determinism-vs-probability`، `quantum-physics-vs-quantum-mechanics`، `coin-vs-dice-quantum-uncertainty`، `quantum-analogy-exercise-boundary`، `quantum-understanding-achievement`.
  - **۸ حذف طبق سیاست «تکراری هارا حذف کن»:**
    * اسلاگ قبلاً منتشر شده در سایت: `many-worlds-interpretation` و `copenhagen-interpretation` (هر دو با fetch زندهٔ سایت تأیید شدند).
    * تداخل با مقالهٔ تحویل‌شدهٔ کرپوس: `تله‌پورت کوانتومی` (مقالهٔ ۱ `quantum-teleportation`) و `جان بل و آزمایشی که سال‌ها بعد انجام شد` (با `bell-experiments` تحویل‌شده + `bell-inequality` زنده).
    * موضوع قبلاً در مقالهٔ منتشرشده پوشش داده شده: `تفاوت انرژی در فیزیک و عرفان‌گرایی` (`everything-is-energy-claim`)، `تفاوت برهم‌نهی با «چندین احتمال»` (`quantum-superposition`)، `تفاوت درهم‌تنیدگی با تله‌پاتی` (`quantum-entanglement-explained`)، `تفاوت ناظر انسانی و ناظر دستگاه` (`quantum-measurement`).
  - شمارنده: **۲۰ از ۸۰**؛ ۷۴ فایل باقی مانده.
* زاویه‌های مکمل تحویلی‌ها: جبرگرایی ← دیو لاپلاس + حد کلاسیکی کوانتوم (مکمل `decoherence`/`wave-function` زنده)؛ فیزیک/مکانیک ← تفاوت اصطلاحی چتر و هسته؛ تاس/سکه ← نقد خود تمثیل‌ها و اصل عدم‌قطعیت؛ تمرین تمثیل ← تمرین ذهنی ۴قدمی با نمونهٔ کارشدهٔ تونل‌زنی؛ جمع‌بندی نهایی ← نقل‌قول فاینمن و «نفهمیدنِ درست».
* واقعیت‌های سنددار: نقل‌قول فاینمن (Character of Physical Law 1965)، لاپلاس ۱۸۱۴، بل ۱۹۶۴، هایزنبرگ ۱۹۲۷.
* ۵ تصویر شاخص جدید: q_109_atomicclock، q_22_standardmodel، q_14_complementarity، q_160_brain، q_135_sunlight.
* `tools/build_contact_sheet.py` جدید: بازسازی خودکار شیت تماس (اکنون ۶۱ تصویر) و `images-featured-webp.zip` (۵.۴ مگابایت). README تصاویر با ۱۷ ردیف تکمیل شد (۱۲ تحویل قبلی که ثبت نشده بودند + ۵ جدید).

---

## ۳۷. افزونه QPedia Importer — ایمپورت یک‌بارهٔ ۲۰ مقاله به‌صورت پیش‌نویس — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک، افزونهٔ تک‌فایلی `qpedia-importer/qpedia-importer.php` ساخته شد (+ `qpedia-importer.zip` و README).
* رفتار: با فعال‌سازی، در اولین بازدید پیشخوان به‌صورت خودکار هر ۲۰ مقالهٔ تحویل‌شده را ایمپورت می‌کند — وضعیت **draft**، عنوان و اسلاگ دقیق، متن HTML نهایی، چکیده در فیلد Excerpt (متای سئو خودکار توسط قالب)، دسته/زیردسته با تطبیق اسلاگِ زندهٔ سایت (ساخت خودکار در صورت نبود).
* ایمنی: مقاله‌های منتشرشده هرگز دست نمی‌خورند (رد + گزارش)؛ اجرای دوباره امن است (پیش‌نویس موجود به‌روزرسانی، تازه ساخته می‌شود)؛ checksum داخلی crc32 جلوی ایمپورت فایل ناقص را می‌گیرد.
* دیتا به‌صورت جاسازی‌شده (JSON ۱۹۸KB → deflate ۴۷KB → base64 در فایل ۷۵KB) — بدون وابستگی به شبکه.
* تصویر شاخص خودکار اختیاری (پیش‌فرض خاموش): ثابت `QPEDIA_IMP_FEATURED_IMAGES` → دانلود از `images-featured-webp/` روی شاخهٔ کاری گیت‌هاب.
* صفحهٔ مدیریتی «QPedia Importer» با گزارش و دکمهٔ اجرای دوباره؛ پیام نتیجه بعد از ایمپورت خودکار.
* سینتکس فایل با پارسر tree-sitter-php تست شد (۰ خطا)؛ صحت payload جاسازی‌شده و صفر ZWNJ در هر ۲۰ مقاله راستی‌آزمایی شد.
* ابزار ساخت: `tools/build_importer_payload.py` (استخراج ۲۰ مقاله از `آماده/` → payload → فایل افزونه).

## ۳۸. افزونه QPedia Featured Images — نصب خودکار تصویر شاخص ۶۲ مقاله — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک («تصاویر شاخص این ۶۲ مقاله رو با افزونه روی مقاله ها بذار… ایمپورت تنها تصویر شاخص»)، افزونهٔ `qpedia-featured-images/` ساخته شد (+ `qpedia-featured-images.zip` در ریشه، ۵.۶ مگابایت).
* مانیفست ۶۲ اسلاگ ← ۶۱ فایل WebP واترمارک‌دار (`images-featured-webp/`): همهٔ ۶۲ پست منتشرشدهٔ زندهٔ سایت (fetch امروز wp-json) یک‌به‌یک پوشش داده شده‌اند؛ استثنا: دو پست منتشرشدهٔ همنام «کوانتوم و پزشکی جایگزین» (id 511 و 516 با اسلاگ `quantum-alternative-medicine-science-2`) هر دو یک تصویر می‌گیرند.
* رفتار: فقط پست‌های منتشرشدهٔ `quantum_article`؛ فقط وقتی تصویر شاخص **ندارد** (تصاویر دستی قبلی هرگز جایگزین نمی‌شوند مگر با دکمهٔ صریح «نصب مجدد همه»). پیش‌نویس‌ها دست نمی‌خورند.
* تصاویر داخل خود افزونه بسته‌بندی شده‌اند (بدون نیاز به اینترنت)؛ اگر فایلی در پوشه نبود، از `images-featured-webp/` روی شاخهٔ کاری گیت‌هاب دانلود می‌شود (۲ آدرس جایگزین).
* اجرا: با فعال‌سازی، خودکار در هر بار باز شدن پیشخوان دسته‌ای ۱۵تایی نصب می‌کند تا تمام شوند؛ اگر یک دور هیچ پیشرفتی نداشت، حالت خودکار متوقف می‌شود (اعلان راهنما). منوی «تصاویر شاخص»: جدول وضعیت ۶۲ اسلاگ + دکمه‌های «نصب همه باقیمانده‌ها»، «نصب مجدد همه (جایگزینی)»، «بررسی دوباره وضعیت».
* متن alt تصویر شاخص = عنوان مقاله + «- QPedia» (سئو).
* اعتبارسنجی: tree-sitter-php ۰ خطا روی هر دو فایل PHP؛ ZWNJ=۰؛ zip سالم (testzip OK)؛ تطابق مانیفست با ۶۲ اسلاگ زندهٔ منتشرشده تأیید شد.
* ابزار ساخت: `tools/build_featured_plugin.py` (تولید `manifest.php` + کپی تصاویر + ساخت zip + راستی‌آزمایی).

## ۳۹. کرپوس ۳۱: «جمع بندی پنج گانه: نقشه ذهنی برای نفهمیدن درست کوانتوم» — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک: «عنوان شماره ۳۱ را بیاور، اما خودت مقاله را بنویس» — متن منبع کنار گذاشته شد (فقط عنوان استفاده شد) و مقاله از نو با اصول خانه نوشته شد.
* **بدون تصویر شاخص** — دستور جدید مالک: «تصویر شاخص اصلا دیگه کار نکن؛ هیچ تصویری برای مقاله کار نکن، تمرکز روی کد و لینک داخلی و منابع». از این به بعد در هیچ مقاله‌ای تصویر ساخته نمی‌شود.
* زاویهٔ مکمل: مقالهٔ ۳۰ (`quantum-understanding-achievement`، منتشرشده) چرایی را می‌گوید؛ این مقاله چگونگی را — نقشهٔ ذهنی پنج ایستگاهی (رفتارها به جای فرمول ← تمثیل پل است نه مقصد ← مرز علم و قصه ← سوال خوب ← شانه‌های غول‌ها). اسلاگ: `quantum-fivefold-mental-map`؛ دسته: مبانی (سطح اصلی).
* ۱۹ لینک داخلی به ۱۲ اسلاگ منتشرشدهٔ زنده (برهم‌نهی، عدم قطعیت، درهم‌تنیدگی، گربهٔ شرودینگر، آزمایش دو شکاف، اندازه‌گیری، جنگ تفسیرها، تمرین تمثیل، همه‌چیز انرژی است، پزشکی جایگزین، اشیای بزرگ، نفهمیدن درست). ۳ منبع معتبر: نامهٔ نیوتن به هوک ۱۶۷۵، فاینمن Character of Physical Law 1965، فاینمن Lectures Vol. III.
* اعتبارسنجی: ۴ آکاردئون FAQ، ۸ تیتر H2، منابع آخر، ZWNJ=۰، ۱۱۲۹ کلمه. خروجی: `آماده/نقشه-ذهنی-پنج-گانه-...md` + `.html`. شمارنده: **۲۱ از ۸۰**.

## ۴۰. کرپوس ۳۲ تا ۴۰ — تحلیل، ۶ حذف تکراری، ۳ مقالهٔ نو — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک: «۳۲ تا ۴۰ رو تحلیل کن و بعد از حذفیات تکراری مابقی را طبق اصول دستورالعمل بنویس». هر ۹ منبع خوانده شد و کاندیدهای تکراری با سایت زنده چک شدند (fetch wp-json).
* **۳ مقالهٔ نو (بدون تصویر شاخص، طبق دستور مالک):**
  - `quantum-sensors` (فناوری ← فناوری روزمره): خط‌کش طبیعی ترازهای انرژی؛ سه اهرم دقت؛ ساعت اتمی/جی‌پی‌اس و ام‌آر‌آی به‌عنوان حسگرهای فعال؛ گرانش‌سنج و قنات‌های ایران؛ مرز شبه‌علم. ۸ لینک زنده، منابع: BIPM 2019، Degen et al. RMP 89 2017، Essen & Parry Nature 1955.
  - `entanglement-quantum-computers` (فناوری ← رایانش و ارتباطات): درهم‌تنیدگی موتور واقعی (نه برهم‌نهی)؛ شور ۱۹۹۴؛ سیکامور ۲۰۱۹؛ ویلو ۲۰۲۴ (تصحیح خطا زیر آستانه)؛ سه اغراق پرتکرار (امتحان یک‌جای همهٔ جواب‌ها / جهان‌های موازی / جایگزینی کامپیوتر شخصی). ۷ لینک زنده.
  - `dirac-antimatter` (تاریخ و آزمایش‌ها ← تاریخ): معادلهٔ دیراک با جملهٔ استاندارد فرمول؛ نقل‌قول تأییدشدهٔ دیراک ۱۹۲۹؛ اندرسون ۱۹۳۲ و نوبل‌ها؛ پوزیترون در PET؛ مرز تخیل. ۵ لینک زنده.
* **۶ حذف طبق سیاست «تکراری هارا حذف کن»** (دقیقاً همان موارد ثبت‌شده در فهرست): دوقلوهای درهم‌تنیده، دو در بسته، نامه، دنیای نیوتنی/پلانک، رابطه فروپاشی و دوشکاف، رمزنگاری کوانتومی (منبع خودش اسلاگ مقالهٔ زندهٔ سایت را دارد).
* اعتبارسنجی هر سه: ۴ آکاردئون FAQ پُر، H2 استاندارد، منابع آخر، ZWNJ=۰، همهٔ لینک‌ها به اسلاگ‌های منتشرشدهٔ زنده (بررسی خودکار). شمارنده: **۲۴ از ۸۰**؛ **۶۸ فایل منبع** باقی مانده.
* محیط گیت بین جلسات ریست شده بود (HEAD محلی `65cabfe`) — طبق پروتکل، fetch + `reset --hard` به نوک ریموت (`6cd30ee`) و ادامهٔ کار؛ بدون force.

## ۴۱. کرپوس ۴۱ تا ۵۰ — قواعد جدید مالک، ۷ حذف تکراری، ۳ مقالهٔ منحصر‌به‌فرد — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* **قواعد جدید مالک (از این به بعد در همهٔ مقالات):** (۱) «همدوسی/واهمدوسی» یک واژهٔ کامل است و جدا نوشته نمی‌شود؛ (۲) منابع آخر فقط لینک تمیز خارجی از وب‌سایت‌های معتبر — بدون هیچ متن توضیحی (هر ۱۷ لینک استفاده‌شده زنده راستی‌آزمایی شد: NobelPrize.org، Britannica، Nature، APS، Royal Society، Springer، arXiv، BIPM، Caltech)؛ (۳) حداکثر ۶ لینک داخلی در هر مقاله، مگر اینکه نباشد؛ (۴) انکر لینک فقط اسم خالص (نه «اندازه‌گیریست» و صیغه‌های فعلی)؛ (۵) مقالات با موضوع/محتوای یکسان حذف شوند — فقط منحصر‌به‌فردها نوشته شوند.
* **تحلیل ۴۱ تا ۵۰ (هر ۱۰ منبع خوانده شد؛ کاندیدها با سایت زنده چک شدند):**
  - **نوشته شد:** `forgotten-women-quantum` (مایر/وو/مایتنر/نوتر؛ نقل‌قول تأییدشده از زندگینامهٔ رسمی نوبل؛ ۲ لینک داخلی، ۴ منبع لینک‌دار)، `feynman-quantum-explainer` (نقل‌قول «تنها راز» از جلد سوم درس‌های فاینمن — متن اصلی واکشی و تطبیق شد؛ ۴ لینک داخلی)، `schrodinger-life-equation` (زندگینامهٔ مکمل `schrodinger-cat` زنده؛ نقل‌قول از مقالهٔ ۱۹۳۵؛ فرمول با جملهٔ استاندارد؛ ۳ لینک داخلی).
  - **حذف شد (۷):** سوءتفاهم ذهن (`mind-quantum-reality` زنده)، عدم‌قطعیت و ابزار (`heisenberg-uncertainty-principle` زنده)، فروپاشی تابع موج (`quantum-measurement` + `wave-function` + `superposition-explained` زنده)، فوتون (عین `photon` زنده)، لیزر (`how-lasers-work` زنده)، مرز شبه‌علم درهم‌تنیدگی (`entanglement-myths` زنده)، مرز کوانتوم/کلاسیک (`decoherence` زنده).
* **اصلاح گذشته‌نگر مقالات ۳۱/۳۲/۳۶/۳۸ طبق قواعد جدید:** لینک‌های داخلی به ۶ رسید (۳۱ از ۱۹ به ۶)، «واهم دوسی» → «واهمدوسی»، انکرها تمیز شدند، منابع به لینک تمیز تبدیل شدند. همه دوباره اعتبارسنجی شدند: ۶/۶/۶/۵ لینک داخلی زنده، ZWNJ=۰، آکاردئون‌ها پُر، منابع فقط لینک خارجی معتبر.
* شمارنده: **۲۷ از ۸۰**؛ فایل‌های منبع: ۶۸ ← **۶۱**. بدون تصویر شاخص (دستور مالک).

## ۴۲. کرپوس ۵۱ تا ۶۰ — ۷ حذف تکراری، ۳ مقالهٔ منحصر‌به‌فرد — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک: «ادامه بده ده تا با هم» — همهٔ قواعد جدید (بدون تصویر، ≤۶ لینک داخلی، منابع فقط لینک تمیز زنده، «واهمدوسی» یک واژه، انکر خالص) اعمال شد.
* **نوشته شد (۳):** `quantum-learning-resources` (نقشهٔ سه‌پله‌ای یادگیری: فاینمن جلد ۳ + MIT OCW 8.04 + IBM Quantum Learning؛ هر سه لینک زنده راستی‌آزمایی شد)، `einstein-bohr-debate` (سه دور نبرد + داوری بل؛ منابع: مقالهٔ EPR 1935، پاسخ بور 1935، مقالهٔ بل 1964 — هر سه در APS، زنده)، `max-planck-blackbody` (زاویهٔ زندگینامه‌ای مکمل `ultraviolet-catastrophe`؛ نقل‌قول «عمل ناامیدانه» از نامهٔ پلانک به وود ۱۹۳۱؛ منابع: زندگینامهٔ رسمی نوبل + بریتانیکا).
* **حذف شد (۷):** مصاحبهٔ اینستاگرامی، مصاحبهٔ خیالی فاینمن، برهم‌نهی ماکروسکوپی، مرز تمثیل گربه، مکان و تکانه، نقد کتاب «راز»، هایزنبرگ و عدم‌قطعیت — هر کدام با ذکر اسلاگ‌های منتشرشدهٔ پوشش‌دهنده در فهرست.
* تمیزکاری: ردیف تکراری `dirac-antimatter` در درختچه حذف شد.
* شمارنده: **۳۰ از ۸۰**؛ فایل‌های منبع: ۶۱ ← **۵۴**. شمار مقالات آمادهٔ انتشار از #۳۱ به بعد: **۱۰ از ۲۰** (آستانهٔ ایمپورتر بعدی طبق دستور مالک).

## ۴۳. کرپوس ۶۱ تا ۷۰ — ۹ حذف تکراری، ۱ مقالهٔ منحصر‌به‌فرد — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک: «ادامه» — ادامهٔ خط‌ده‌تایی با قواعد ثابت (بدون تصویر، ≤۶ لینک داخلی، منابع فقط لینک تمیز زنده، ZWNJ=۰، آکاردئون ۴تایی).
* **نوشته شد:** `solar-cells-photoelectric` (فناوری ← فناوری روزمره) — مسیر از ایدهٔ ۱۹۰۵ اینشتین تا پنل مدرن: فلز → نیمه‌هادی، شکاف انرژی، اتصال p-n، بازده ~۲۰٪. منابع زنده: DOE آمریکا + بریتانیکا (سولارسل + اثر فوتوالکتریک)؛ عدد بازده با متن بریتانیکا تطبیق داده شد. ۵ لینک داخلی زنده.
* **حذف شد (۹):** ۶۲ عدم‌قطعیت بهانهٔ غیرعلم، ۶۳ فهمیدن/کارکردن، ۶۴ مشاهده = برهم‌کنش، ۶۵ کوانتوم در تبلیغات، ۶۶ اشیای روزمره و دیکوهرنس، ۶۷ قدرت ذهن، ۶۸ عمل شبح‌وار، ۶۹ دانشمندان و عدم‌قطعیت، ۷۰ همه‌چیز متصل عرفانی — هر کدام با اسلاگ‌های زندهٔ پوشش‌دهنده در فهرست.
* شمارنده: **۳۱ از ۸۰**؛ فایل‌های منبع: ۵۴ ← **۴۵**. مقالات آمادهٔ انتشار از #۳۱ به بعد: **۱۱ از ۲۰** (آستانهٔ ایمپورتر بعدی).

## ۴۴. پایان کرپوس — سری ۸۴ مقاله کامل شد — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک: «تا آخرین مقاله این سری را کامل کن. که تموم بشه بخش مقالات» — دستهٔ پایانی (۷۱ تا ۸۲) به همراه ثبت نهایی همهٔ ردیف‌های باقی‌ماندهٔ فهرست.
* **نوشته شد (۲):**
  * `why-quantum-math-works` (ریاضیات و فرمالیسم کوانتومی) — چرا ریاضیات کوانتوم تا بیش از ده رقم اعشار با آزمایش هم‌خوان است ولی فهم شهودی‌اش سخت است؛ سؤال ردیف ۳ (چقدر ریاضی لازم داریم؟) در بدنه و پرسش‌های متداول پوشش داده شد. دستهٔ خالی `mathematics` با همین مقاله پر شد. منابع زنده: MIT OCW 8.04 + 18.06 (استرنگ) + Feynman Lectures III.
  * `quantum-computer-reality` (فناوری ← رایانش و ارتباطات کوانتومی) — مقدمهٔ عمومی کامپیوتر کوانتومی که سایت هنوز نداشت: بیت در برابر کیوبیت، سایکامور ۲۰۱۹، ویلو ۲۰۲۴، دوران NISQ و تصحیح خطا، مرز شعار و واقعیت. منابع زنده: Google Quantum AI، IBM Think، Nature (Willow)، arXiv (Shor).
* **حذف شد (۱۲):** ۷۲ عدم‌قطعیت=همه‌چیز ممکن، ۷۳ مدرسه‌ها، ۷۴+۷۵ دو مقالهٔ تمثیل، ۷۶ شبه‌علم در یک جمله (کپی عین `spot-pseudoscience-one-sentence`)، ۷۷ رمزنگاری، ۷۹ سولوی ۱۹۲۷ (پوشش در `einstein-bohr-debate` تحویل‌شده)، ۸۰ گوشی موبایل، ۸۱ پزشکی (کپی عین `quantum-alternative-medicine-science`)، دو فایل گربه (۸۲ + نسخهٔ HTML).
* **ثبت حسابداری فهرست:** ۷ ردیف ایمپورتر شماره ۱ (۲، ۲۰، ۲۲، ۲۶، ۲۷، ۲۸، ۳۰) و ۷ حذف دسته‌های اولیه (۱۸، ۱۹، ۲۱، ۲۳، ۲۴، ۲۵، ۲۹) علامت‌گذاری شدند.
* **آمار نهایی سری:** ۸۲ ردیف فهرست — ✅ نوشته/تحویل‌شده ۳۳، ❌ حذف ۴۹. فایل‌های منبع: ۴۴ ← **۳۲** (فقط منابع مقالات تحویل‌شده). مقالات آمادهٔ انتشار از شماره ۳۱: **۱۳ از ۲۰** — ایمپورتر شماره ۲ در انتظار تصمیم مالک.

## ۴۵. ایمپورتر شماره ۲ (۱۳ مقالهٔ سری دوم) — ۱۲ شهریور ۱۴۰۵ / ۱ سپتامبر ۲۰۲۶

* به دستور مالک: «ایمپورتر رو بزن» — ساخته شد: پوشهٔ `qpedia-importer-2/` + زیپ `qpedia-importer-2.zip` (ریشهٔ ریپو).
* **۱۳ مقالهٔ آماده از شماره ۳۱ به بعد** (به ترتیب تحویل): `quantum-fivefold-mental-map`، `quantum-sensors`، `entanglement-quantum-computers`، `dirac-antimatter`، `forgotten-women-quantum`، `schrodinger-life-equation`، `feynman-quantum-explainer`، `quantum-learning-resources`، `einstein-bohr-debate`، `max-planck-blackbody`، `solar-cells-photoelectric`، `why-quantum-math-works`، `quantum-computer-reality`.
* **دسته‌های جدید:** `everyday-tech` (فناوری روزمره، زیر فناوری) و `mathematics` (ریاضیات و فرمالیسم کوانتوم) به نقشهٔ دسته‌ها اضافه شدند؛ builder برگِ دسته را از آخرین بک‌تیک خط «دسته» می‌خواند.
* **هم‌زیستی با ایمپورتر #۱:** نام توابع/ثابت‌ها `qpedia_imp2_*`/`QPEDIA_IMP2_*`، اسلاگ منو `qpedia-importer-2`، نام افزونه «QPedia Importer 2». فایل PHP کاملاً بدون ZWNJ (۳۳ نیم‌فاصله پاک شد)؛ ساختار کد با #۱ byte-level یکسان به‌جز پیلود و این بازنام‌گذاری‌ها (diff تأیید شد).
* پیلود: ۱۲۵٬۷۴۹ بایت JSON → ۳۰٬۸۴۲ بایت deflate → CRC `840716082`؛ round-trip تأیید شد؛ پیلود داخل PHP با `payload.b64` یکسان.
* وضعیت پس از سری: مقالات زندهٔ یکتا ۶۱؛ تحویل‌شده ۳۳؛ دانشمندان زنده ۲۳ → ۳۲ نفر از فهرست ۵۰ باقی؛ ذرات بنیادی: ۲ منتشرشده + ۱ نسبی + ۷ باقی. یادداشت روزآمد در «فهرست هدف و پیشرفت».
