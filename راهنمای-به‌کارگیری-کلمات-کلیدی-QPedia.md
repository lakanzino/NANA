# راهنمای به‌کارگیری کلمات کلیدی گوگل در مقالات قدیم و جدید — QPedia

**تاریخ:** ۲۱ شهریور ۱۴۰۵ / ۱۲ سپتامبر ۲۰۲۶
**مخاطب:** ویراستار محتوای qpedia.ir
**پیش‌نیاز:** این سند مکمل `دستورالعمل-مقاله-نویسی-QPedia.md` است؛ همهٔ قواعد آن (HTML ساده، بدون ZWNJ در متن تحویلی، ۳ تا ۶ لینک داخلی فقط به مقالات ✅، منبع واقعی، سقف ۱۸۰۰ کلمه) روی کارهای این سند هم حکم می‌کند.

> سؤال اصلی: «خامِ کلمات کلیدی مردم را گرفته‌ام و تقویم چیده‌ام — حالا چطور این کلمات را **داخل مقالات قدیمی و جدید** به کار ببرم تا ترافیک بگیرد؟»
> پاسخ کوتاه: **هر خوشه کلمه = دقیقاً یک URL.** کلمه‌های خوشهٔ آن URL در شش سطحِ مشخص از همان مقاله پخش می‌شوند؛ اگر URL وجود ندارد، مقالهٔ جدید ساخته می‌شود؛ اگر دو URL یک خوشه را می‌خواهند، ادغام می‌شوند. بقیهٔ سند روشِ اجراییِ همین جمله است.

---

## بخش ۰ — قانون‌های پایه (قبل از هر ویرایش)

1. **یک خوشه، یک صفحه.** اگر دو مقالهٔ سایت برای یک کلمه رتبه بگیرند، گوگل بینشان مردد می‌ماند و هر دو می‌بازند ( cannibalization). تکلیف را با ادغام یا ۳۰۱ روشن کنید.
2. **کلمه کلیدی جایگزین محتوا نمی‌شود، جهت می‌دهد.** چیزی که کلمات به مقالهٔ قدیمی اضافه می‌کنند **پاسخ به سؤالاتی است که هنوز در آن مقاله جواب ندارند**. فقط تکرار کلمه در متن = اسپم؛ بدون فایده، با ضرر.
3. **صفحه‌ای که رتبهٔ ۱–۳ دارد و CTR مناسبی می‌گیرد دست‌نخورده می‌ماند** (عنوان و URL عوض نمی‌شود؛ فقط افزودن محتوا مجاز است).
4. **کلمات غلط املایی را در متن نیاورید** («کوانتم» نه). نرمال‌ساز املای گوگل خودش رسیدگی می‌کند؛ متن شما باید معیار بماند (E-E-A-T).
5. **نیم‌فاصله نترسید.** گوگل «درهم‌تنیدگی» و «درهم تنیدگی» را یکی می‌بیند؛ قاعدهٔ بدون-ZWNJِ سایت را برای سئو نشکنید.
6. **برندهای لاتین را دوشکل بنویسید:** فارسی + لاتین در پرانتز — چون بخشی از کوئری‌های خام مردم لاتین است: «ویلو (Willow)»، «آی‌بی‌ام (IBM)».
7. **دو املا رایج فارسی، دو جای مختلف:** در عنوان «کامپیوتر»، در بدنه و H2ها «رایانش» (یا برعکس) — هر دو شکل در کوئری‌های خام وجود دارد؛ کل‌کل نکنید که هر دو در تیتر جا شوند.

---

## بخش ۱ — مسیر داده خام تا ویرایش (هفت مرحله)

**ورودی:** گزارش خام کوئری‌ها (Search Console → Performance → Query، ترجیحاً ۱۶ ماه کامل، یا خروجی ابری که از آن گرفتید) + `نگاشت-خوشه‌های-کلیدی-به-مقالات-QPedia.csv`.

| # | مرحله | ابزار/معیار |
|---|---|---|
| ۱ | **پاک‌سازی خام** | حذف کوئری‌های برندی که مالک نیستید، حذف کوئری‌های کاملاً بی‌ربط (مثلاً «کوانتوم در بازی» اگر بازی سایت نیست)، حذف زیر ۱۰۰ ایمپرشن مگر ترند صعودی |
| ۲ | **قصد (intent) تگ** | هر کوئری یکی از این‌ها: `چیست` / `به زبان ساده` / `X و Y` / `تفاوت` / `چگونه` / `اخبار/به‌روز` / `کتاب-منبع` / `نقد شبه‌علم` |
| ۳ | **خوشه‌بندی** | کوئری‌هایی که جوابشان یک صفحه است، یک خوشه؛ نه بر اساس شباهت ظاهری کلمات، بر اساس **یک‌قصدِ یک‌جواب** |
| ۴ | **مطابقت با موجودی سایت** | هر خوشه به درختچه (`درختچه-کامل-مقالات-QPedia.md`) زده می‌شود → سه وضعیت: **پوشش‌داده‌شده** (مقالهٔ قدیمی هست) / **نیمه‌پوشش** (هست ولی این زاویه ندارد) / **گپ** (نیست) |
| ۵ | **تخصیص URL** | خوشه → یک اسلاگ. ستونِ «مقصد» در CSV نگاشت. خروجی مجازِ این مرحله فقط سه فعل است: **رفرش / توسعه / مقالهٔ جدید** |
| ۶ | **کارlist** | اجرا: `python3 build_keyword_worklist.py --gsc gsc-export.csv --map نگاشت-...csv` → برای هر URL فهرست کار می‌دهد (بخش ۲ و ۳) |
| ۷ | **اجرا + اندازه‌گیری** | ویرایش‌ها طبق بخش ۲/۳؛ پس از انتشار، خطِ «به‌روزرسانی: تاریخ» در صفحه؛ بازبینی نتایج ۴ تا ۸ هفته بعد (بخش ۵) |

**نکتهٔ مرحلهٔ ۴:** «کلماتی که سایت شما برایشان **صفحهٔ دهم یا یازدهم** است هم گپ حساب می‌شوند، نه پوشش. رتبهٔ ۵۰+ با رتبهٔ ناموجود تفاوت عملی ندارد؛ تصمیم با نوشتن مقالهٔ درست است، نه امید به بالا آمدن صفحهٔ اشتباه.

---

## بخش ۲ — کلمات کلیدی در **مقالات قدیمی**: شش سطح ویرایش

هر مقالهٔ قدیمی که خوشه‌ای به آن تخصیص داده شده، فقط از این شش جا ویرایش می‌شود (به همین ترتیب اولویت؛ معمولاً ۲ تا ۴ سطح کافی است):

### ۲.۱ سؤالاتِ کوئری → تیتر H2 تازه (مؤثرترین کار)
کوئری‌های «چیست / چگونه / چرا / آیا»ی خوشه را **عیناً** H2 کنید و زیرش ۴۰ تا ۸۰ کلمهٔ جدید بنویسید. یک بلوک ۶۰ کلمه‌ایِ هدفمند در مقالهٔ قدیمی، اغلب بیشتر از بازنویسی کل مقاله رتبه جابه‌جا می‌کند — چون گوگل حالا **بندِ مستقل** برای آن کوئری دارد، نه یک کلمهٔ سرگردان در پاراگراف.

> مثال واقعی سایت: `/what-is-quantum/` (کوانتوم یعنی چه؟) — اگر خامِ کوئری‌ها نشان دهد «کوانتوم چیست به زبان ساده» و «علم کوانتوم یعنی چه» پر‌تکرارند ولی صفحهٔ ما فقط «یعنی چه» را پوشش می‌دهد: افزودن یک H2 به همین نام‌ها + دو پاراگراف، و افزودن عبارت «کوانتوم چیست» به جملهٔ دوم لید.

### ۲.۲ آکاردئون سؤالات متداول (details/summary)
سه تا شش مورد از «People Also Ask» / پرسش‌های مرتبطِ همان خوشه. پاسخ هر سؤال: دو تا چهار جمله، کامل و مستقل (تکه‌برای Featured Snippet). این سطح کم‌هزینه‌ترین جا برای گرفتن کوئری‌های دم‌بلند است و قالب QPedia از قبل `details/summary` می‌پذیرد.

### ۲.۳ لید (پاراگراف اول)
عبارت اصلی خوشه باید در **۱۰۰ کلمهٔ اول** حداقل یک‌بار، طبیعی و دقیقاً بیاید (نه لیست کلمات). اگر لید فعلیarticle موضوع را با زاویهٔ دیگری شروع می‌کند، جملهٔ دوم بازنویسی می‌شود — قلاب دست‌نویس حفظ شود.

### ۲.۴ جدول/لیست مقایسه‌ای برای کوئری‌های «X یا Y / تفاوت»
کوئری‌های مقایسه‌ای (مثل «تفاوت فیزیک کوانتوم و مکانیک کوانتومی») با متن روان گرفته نمی‌شوند؛ یک جدول `ul`/متنیِ مقایسه‌ای + جمع‌بندی تک‌جمله‌ای لازم است.

### ۲.۵ عنوان و متا — فقط با توجیه داده‌ای
- **اگر** رتبهٔ ۱–۵ ولی CTR زیر ~۲٪ → عنوان را با modifierِ کوئری اصلاح کنید («به زبان ساده»، «راهنمای کامل»، عدد سال برای اخبار).
- عنوان همیشه ≤ ۶۰ کاراکتر، فارسی، شامل کلمهٔ اصلی.
- توضیح متا (همان چکیدهٔ وردپرس که `functions.php` خودکار به تگ meta تبدیل می‌کند): ۱۲۰–۱۵۵ کاراکتر، با کلمهٔ اصلی + یک دلیل کلیک («بدون ریاضی»، «با مثال ایرانی»).
- **URL و اسلاگ عوض نمی‌شود** مگر اسلاگ فارسی/خراب باشد؛ اگر عوض شد، ریدایرکت ۳۰۱ ضروری است.

### ۲.۶ لینک داخلی با انکورِ خودِ کلمه (بک‌لینک داخلی)
مقالهٔ قدیمی تنها با ویرایش خودش بالا نمی‌آید؛ **لینک‌های ورودی با انکورِ دقیقِ کوئری** سوخت رتبه است:
- از هر مقالهٔ مرتبطِ منتشرشده، یک لینک با انکورِ خوشه به صفحهٔ مقصد (حداکثر ۲–۳ لینکِ ورودیِ تازه در هر رفرش؛ بیشتر = رقیق‌شدن انکور).
- از صفحهٔ مقصد، ۳–۶ لینک خروجی طبق قاعدهٔ سایت، ترجیحاً به خواهرانِ همان خوشه (ستون فقرات topic cluster).
- انکور متنوع باشد: دو بار عینِ کلمه، یک بار طبیعیِ جمله‌ای («اینجا دقیقاً توضیح داده‌ایم که کامپیوتر کوانتومی چگونه کار می‌کند»).

### چه‌وقت مقالهٔ قدیمی را **دست نزنید**
| نشانهٔ صفحه | اقدام |
|---|---|
| رتبهٔ ۱–۳ + CTR سالم | فقط افزودن FAQ/بند (سطوح ۲.۱–۲.۲)؛ عنوان/URL ممنوع |
| ترافیک برندی (نام سایت/کتاب شما) | دست‌نخورده |
| رتبهٔ ۴–۱۵ | کاندیدای طلاییِ رفرش کامل شش‌سطحه |
| رتبهٔ ۱۶–۴۰ با ایمپرشن بالا یا نیتِ متفاوت | احتمالاً نیتِ کوئری با صفحه نمی‌خواند → مقالهٔ جدید، نه رفرش |
| دو صفحه برای یک کوئری | ادغام (بهترین‌ها منتقل، ۳۰۱ از صفحهٔ ضعیف) |

---

## بخش ۳ — کلمات کلیدی در **مقالات جدید** (تقویم)

برای هر مقالهٔ جدیدِ تقویم، ترتیبِ کلمات در ساختار قفل است (بقیه‌اش کارِ همیشگیِ دستورالعمل است):

1. **کلمهٔ اصلی خوشه = تیتر.** سؤالِ پرتکرارترین کوئری، با همان دستور جمله، زیر ۶۰ کاراکتر.
2. **سه–چهار عضو بعدی خوشه = H2ها** (هر بخش به H2 تبدیل می‌شود، نه H1).
3. **دم‌بلندها = FAQ آکاردئونی** (۴ مورد، مطابق چک‌لیست تحویل).
4. **اسلاگ لاتین کوتاه = ترجمهٔ همان کلمه** تا هویت موضوعی URL تمیز بماند (`what-is-quantum-physics`، نه `article-77`).
5. **قانون ستون (pillar):** هر مقالهٔ پایه مثل `what-is-quantum` **هاب** است؛ مقالات فرزند به آن **بالا-لینک** می‌دهند (با انکورِ کلمهٔ مادر) و مادر از جایشان **پایین-لینک** می‌گیرد. ترتیب انتشار را رعایت کنید: مادر زنده باشد بعد فرزند منتشر شود (قاعده ۳ خط قرمز سایت).
6. **خوشهٔ خبری/به‌روز = یک صفحهٔ زنده + آپدیت ماهانه، نه رفرش خبریِ پراکنده.** «کامپیوتر کوانتومی؛ وضعیت گوگل، IBM، چین» را به‌عنوان یک صفحهٔ به‌روزرو با H2 «آخرین تحولات» نگه دارید؛ هر ماه رفرش می‌شود و تاریخ به‌روز می‌خورد. صفحات خبریِ تکه‌تکه در سایت آموزشیِ کوچک، ارزش افزونگی ندارند و بودجهت خزش را می‌سوزانند.
7. **تاریخ + «به‌روزرسانی» برای کوئری‌های سال‌دار:** برای خوشه‌هایی که مردم سال می‌زنند (نوبل، ویلو، «آخرین وضعیت»)، تاریخ انتشار و آخرین به‌روزرسانی باید در صفحه دیده شود — سیگنال tims Fresher است؛ اما هر عددِ به‌روز شده **باید راستی‌آزمایی مجدد شود** (قاعده ۲ مرجع)، و اگر راستی‌آزمایی نشد **حذفش کنید نه به‌روزرسانی**.

---

## بخش ۴ — نگاشت اختصاصی تقویم شما به موجودی واقعی سایت

خلاصهٔ تصمیم‌ها در فایل `نگاشت-خوشه‌های-کلیدی-به-مقالات-QPedia.csv` آمده (همان فایل را به `build_keyword_worklist.py` بدهید). مهم‌ترین حکم‌ها:

| خوشهٔ تقویم | وضعیت در سایت | حکم |
|---|---|---|
| کوانتوم چیست / به زبان ساده | ✅ `/what-is-quantum/` منتشرشده | **رفرش** شش‌سطحه؛ تیتر و URL را عوض نکنید |
| فیزیک کوانتوم چیست / علم کوانتوم | گپ (hubsایت «یعنی چه» را دارد نه «فیزیک کوانتوم») | **مقالهٔ جدید** `/what-is-quantum-physics/` با عنوان «فیزیک کوانتوم چیست؟ راهنمای کامل» — مرز با مقالهٔ بالا: آن *مفهوم*، این *رشته* |
| مکانیک کوانتومی چیست | 🆕 `quantum-physics-vs-quantum-mechanics` تحویل ۱۲ شهریور | **انتشار** + تیتر را با «مکانیک کوانتومی چیست» هم‌تراز کن |
| کامپیوتر کوانتومی چیست و چگونه کار می‌کند | 🆕 `quantum-computer-reality` (ایمپورتر ۲) | **انتشار/بررسیِ زنده‌بودن**؛ تیتر: «کامپیوتر کوانتومی چیست؟…» — کیوبیت (`/qubit/` ✅) و رمزنگاری فرزندانش‌اند |
| درهم‌تنیدگی چیست | ✅ `/quantum-entanglement-explained/` | **رفرش**: H2 «به زبان ساده» + لینک دوطرفه با `bell-inequality` (✅) و `entanglement-myths` (صف جدید) |
| گوگل/IBM/ویلو/چین | دو پیش‌نویس در صف: «پیشرفت گوگل در تصحیح خطا»، «IBM Condor» | پیش‌نویس‌ها → **سوختِ آپدیت** صفحهٔ «وضعیت روز»، نه دو مقالهٔ جدا؛ هر عدد با منبع اصلی (Nature/Quantum AI/IBM Think) |
| هوش مصنوعی کوانتومی / QML | 🆕 `does-ai-use-quantum` + پیش‌نویس QML | **ادغام**: مقالهٔ موجود با بخش‌های QML گسترش یابد؛ «quantum machine learning» در پرانتز |
| کامپیوتر کوانتومی در ایران | ❌ گپ کامل | **مقالهٔ جدید** `/quantum-computing-iran/` — با هشدار: هر ادعای عددی/دانشگاهی فقط با منبع راستی‌آزمایی‌شده، وگرنه طبق بخش ۲ مرجع منتشر نکنید |
| کوانتوم و خدا | 🆕 `does-quantum-prove-god` تحویل ۱۲ شهریور | **انتشار**؛ لینک ورودی از `copenhagen-interpretation` و `quantum-measurement` (✅) |
| قانون جذب / ثروت / ذهن | نیمه‌پوشش: `/everything-is-energy-claim/` ✅ + صفِ `the-secret-review` | **رفرش** مقالهٔ قدیمی با H2 «قانون جذب ربطی به کوانتوم دارد؟» + **مقالهٔ مجزای** `/quantum-law-of-attraction/` برای کل خوشه |
| نقد ادعاهای فرد مطرح (قاضی‌پور و…) | ❌ گپ — حساس | فعلاً **منتظارِ تأیید انسانی/حقوقی**؛ فقط با منبع اولیه (ویدیوی بایگانی‌شده + تایم‌کد). این خوشه پتانسیل ترافیک بالا و ریسک حقوقی هم‌زمان دارد |
| کتاب‌های گریفیتس/زتیلی/ساکورایی | 🆕 `quantum-learning-resources` (در ایمپورتر ۲) | **گسترش** با H2 «کتاب‌های درسی معروف» + نام لاتین در پرانتز؛ از `/qubit/` و مقالات مبانی لینک ورودی |
| «مکانیک کوانتومی pdf» و دانلود | — | **هدف‌گذاری نکنید.** لینک PDF دزدی = خلاف حقوق نشر + سیگنال منفی؛ به‌جایش «رایگانِ قانونی» (MIT OCW، آرشیو مقالات باز) را در همان مقاله لینک کنید |
| مستندها / مرد مورچه‌ای | دو پیش‌نویس در صف | **مقالهٔ جدید** — قلاب‌های ترافیکِ بالا برای جمعیتِ غیرمتخصص؛ پلِ لینکی به `decoherence` و `what-is-quantum` |

**نکتهٔ عملیاتیِ آخرِ تقویم شما («هر ماه ۱ پایه + ۲ دیگری»):** یک ترتیبِ امن اضافه می‌کنم — هر ماه را با **یک رفرش از جدول ۲ شروع کنید، بعد یک انتشار، بعد یکی جدید**. رفرش روی دامنهٔ جاافتاده سریع‌ترین بازدهِ سایت‌های یک‌ساله است؛ ریسک رفرش صفر است، ریسک مقالهٔ تازه همان‌جا که خط قرمزها یادآوری می‌کنند.

---

## بخش ۵ — اندازه‌گیری (تا «ترافیک گرفتن» عدد داشته باشد)

1. **پیش از ویرایش:** از GSC فیلترِ `page:qpedia.ir/slug/` → ۴ هفته: جمع impressions، clicks، میانگین رتبه. در یک شیت ثبت شود (ستونِ تاریخِ بیس‌لاین).
2. **معیار موفقیت هر خوشه:** میانگین رتبهٔ کوئری‌های خوشه ≤ ۵، یا impressions همان کوئری‌ها +۳۰٪ در ۸ هفته.
3. بعد از انتشار ویرایش: URL را در **URL Inspection → Request Indexing** بزنید (روی صفحات رفرش‌شده معمولاً چند روز تا دو هفته بعد رتبه تکان می‌خورد).
4. هر ۴ هفته **خروجی تازهٔ GSC** را با اسکریپت بخش ۱/مرحله ۶ بخورانید؛ خروجیِ «گپ‌ها» ورودیِ تقویمِ ماه بعد است. این چرخه، تقویمِ ایستا را به سیستم تبدیل می‌کند.
5. رفرشی که بعد از دو چرخه (۱۶ هفته) هیچ تأثیری نداشت → یا نیت را اشتباه خوانده‌ایم یا شایستهٔ مقالهٔ جداست؛ «رفرشِ دومِ همان‌جور» را تکرار نکنید.

---

## چک‌لیست هر رفرش (کپی شود بالای یادداشتِ هر مقاله)

- [ ] خوشه و کلماتش از CSV نگاشت مشخص است (حداکثر ۵ کلمه برای یک صفحه).
- [ ] ۱ کوئریِ «چیست/چگونه/آیا» به H2 + ۴۰–۸۰ کلمهٔ پاسخِ تازه تبدیل شد.
- [ ] ۳–۶ مورد FAQ از پرسش‌های مرتبط اضافه شد (details/summary، ۴تایی مطابق قالب).
- [ ] عبارت اصلی در ۱۰۰ کلمهٔ اول هست؛ کلمه‌ها لیست‌وار تکرار نشده‌اند.
- [ ] فقط اگر رتبهٔ خوب+CTR بد بود → عنوان اصلاح شد (≤۶۰ کاراکتر)؛ URL عوض نشد.
- [ ] ۲–۳ لینک ورودیِ تازه با انکورِ کلمه از مقالات ✅ گرفته شد؛ لینک‌های خروجی به خواهران خوشه داده شد.
- [ ] چکیده/متا با کلمهٔ اصلی و یک دلیلِ کلیک بازنویسی شد.
- [ ] عدد/ادعای تازه راستی‌آزمایی شد؛ «دست‌کم ۲ منبع زنده» رعایت شد.
- [ ] تاریخ به‌روزرسانی ثبت + Request Indexing زده شد.
- [ ] خطِ بیس‌لاین GSC پیش از ویرایش ثبت شد.
